# Project Log

Use this file for durable decisions and meaningful project events. Keep entries short and chronological.

## 2026-05-30

- Adopted the Studio Pattern from `the-studio-pattern`.
- Source version/date/commit: `2026-05-30 initial scaffold`, `dbbe3e9`.
- Local adaptation: early Grow project scaffold with GitHub connection notes; product scope and runtime stack are still TBD.
- Initial review focus: define product scope, choose runtime stack, confirm GitHub repo visibility and integration model.
- Captured the first product intention in `docs/vision-and-plan.md`: a local Ollama-powered musical terrarium with top-down agents, human avatar interaction, shared listening, instrument invention, and phased band-session behavior.
- Added initial Studio Pattern collaboration intent: Codex as implementation lead, Antigravity and Claude as possible review or creative-drift participants.
- Confirmed browser-first prototype, tonal/modal-plus-rhythm musical language, producer-like human avatar, Gemma 4 31B Ollama target, effects-agent role, and rolling best-moments capture/replay/export direction.
- Added `CLAUDE.md` and `.agent/handoffs/2026-05-30-claude-planning-review.md` so Claude Code on the Mac Mini has a clear review/creative-collaboration entry point.
- Created private GitHub repository `ArneGleason/grow`, configured it as `origin`, and pushed `main`.
- Added initial persistence recommendation in `docs/persistence-checkpoints.md`: SQLite via the local backend, using an append-only event log plus periodic snapshots for rewind, best-moment replay, and branch/fork support.
- Added `.agent/handoffs/2026-05-30-claude-review-and-creative-drift.md` as the next Claude Code/Mac Mini assignment: critique risks, suggest what to change/remove, identify what to protect, propose synergistic creative ideas, and recommend the smallest implementation slice.
- Added `.agent/handoffs/2026-05-30-copy-paste-claude-kickoff.md` so Arne can manually paste a concise Claude Code kickoff prompt while preserving the human-mediated relay step.
- Added explicit `From`, `To`, and `Relay` fields to handoffs so manually copied text preserves authorship, intended recipient, machine context, and Arne's human-routing role.
- Added a standing rule to route reusable Grow learnings back into the canonical Studio Pattern and captured the first cross-project feedback note: addressed handoffs with explicit `From`, `To`, and `Relay` metadata.
- Received Claude Code planning review via Arne's manual relay and preserved the intake in `.agent/reviews/2026-05-30-claude-planning-pass.md`.
- Adopted Claude's scope cuts into `docs/vision-and-plan.md` and `docs/persistence-checkpoints.md`: first build becomes a rule-based PixiJS/Tone.js terrarium; Ollama, SQLite, producer avatar, forks, instrument invention, effects routing, and automatic capture move later.
- Captured Arne's latency direction in `docs/time-and-lookahead.md`: Grow should use a delayed-now/lookahead model where players can listen, think, rehearse, commit future material, and perform later in sync instead of needing hard real-time LLM decisions.
- Captured Arne's session-mode direction in `docs/session-modes.md`: Grow should avoid nonstop generative ambience and instead support breaks, solo practice, rehearsal, performance, reflection, constructed pieces, opt-in participation, and meaningful silence.
- Captured Arne's producer-avatar direction in `docs/producer-proxy.md`: the human should type natural-language prompts, and a producer proxy/interpreter should move, speak, cue, and negotiate in-world on the human's behalf.
- Captured Arne's scope direction in `docs/future-multi-terrarium.md`: Grow remains a solo local instrument for now, but future architecture may allow banking between terrariums/bands and having one group observe or react to another.
- Added `docs/implementation-plan.md` and a Claude review handoff for the first build byte. Proposed Byte 1: Vite/TypeScript + PixiJS/Tone.js page with bounded terrarium, one visible player, one simple sound, start/stop, and status readout.
- Received Claude's Byte 1 implementation-plan review and preserved it in `.agent/reviews/2026-05-30-claude-byte-1-plan-review.md`. Adopted revisions: stationary pulse player, percussive 90 BPM beat, explicit Tone.js lifecycle module, stable test hooks, HMR cleanup, no React, and movement deferred to Byte 2.
- Implemented Byte 1 app scaffold: Vite/TypeScript, PixiJS bounded terrarium, stationary `pulse` player, Tone.js percussive beat, start/stop control, status readout, `window.transport.getState()` hook, and HMR cleanup.
- Validated Byte 1 with clean `npm run build`, clean `npm audit`, and Playwright start/stop smoke testing that confirmed transport scheduling returns to zero after stopping.
- Updated the repo validation command to use `git ls-files --cached --others --exclude-standard` now that ignored `node_modules/` and `dist/` trees make the initial scaffold `find` check noisy.
- Added `.agent/handoffs/2026-05-30-copy-paste-claude-byte-1-implementation-review.md` so Arne can route the pushed Byte 1 implementation to Claude Code for review.
- Added `.agent/handoffs/2026-05-30-copy-paste-claude-code-review.md` with explicit GitHub pull/push guidance, review role boundaries, validation commands, and review-output shape for Claude Code.
- Added `docs/principles/` with listening model, inner music, and subjective taste principles. Updated the implementation plan so Byte 2 becomes a musical event ledger plus minimum listening frame before adding more players.
- Preserved Claude's Byte 1 code review in `.agent/reviews/2026-05-30-claude-byte-1-code-review.md` and landed the pre-Byte-2 fixes: single-source Tone bar updates through `getDraw()`, real scheduled-sequence counting, committed Playwright smoke test, early Start handler attachment, and explicit terrarium aspect fit.
- Implemented Byte 2a: added `src/players.ts` with a first-class `Player` type and `pulse` registry entry, then rendered both the terrarium marker and inspector from that shared player data without adding new behavior.
- Added `.agent/handoffs/2026-05-30-copy-paste-claude-byte-2a-review.md` so Arne can route the pushed Byte 2a player-registry implementation to Claude Code for review.
- Received Claude's Byte 2a code review via Arne's manual relay and preserved it in `.agent/reviews/2026-05-30-claude-byte-2a-code-review.md`.
- Implemented Byte 2: added a bounded musical event ledger, minimum listening-frame summarizer, `GrowWorldState` as the owner of mutable player runtime state, pulse event emission from the Tone sequence, safe DOM-based inspector rendering, generalized player state rendering, and a renamed Grow smoke test.
- Added `.agent/handoffs/2026-05-30-copy-paste-claude-byte-2-review.md` so Arne can route the pushed Byte 2 implementation to Claude Code for review.
- Received Claude's Byte 2 code review via Arne's manual relay and preserved it in `.agent/reviews/2026-05-30-claude-byte-2-code-review.md`. Byte 2 is approved; Byte 3 should fix scheduled event timing jitter, move UI rendering out of the musical event callback, and make the listening frame aware of live transport beat when silence ratios matter.
- Implemented Byte 3: added `pulse`, `bass`, and `melody` as deterministic rule-based players, scheduled three Tone.js sequences, stamped events from scheduled transport time, coalesced UI rendering through `requestAnimationFrame`, passed `currentBeat` into listening frames, and added small visual drift for the three players.
- Added `.agent/handoffs/2026-05-30-copy-paste-claude-byte-3-review.md` so Arne can route the pushed Byte 3 implementation to Claude Code for review.
- Received Claude's Byte 3 code review via Arne's manual relay and fetched the durable artifact from `claude/byte-3-code-review` commit `7c187a6`. Byte 3 is approved; before Byte 4 taste, add a Byte 3b cleanup for coarser player posture state, side-effect-free `window.listening.getFrame()`, non-overlap-double-counting `silenceRatio`, and tonal context as data.
- Implemented Byte 3b: player state now represents recent-participation posture rather than note articulation, note-on activity flashes separately in the terrarium, `window.listening.getFrame()` is side-effect-free, `silenceRatio` uses active interval union, and world state exposes `C mixolydian` tonal context.
- Added `.agent/handoffs/2026-05-30-copy-paste-claude-byte-3b-review.md` so Arne can route the pushed Byte 3b cleanup to Claude Code for review.
- Received Claude's Byte 3b code review via Arne's manual relay and fetched the durable artifact from `claude/byte-3b-code-review` commit `4f5effd`. Byte 3b is approved; follow up with Byte 3c to make the note-on flash visibly render, share the 8-beat window constant, and wire player pitch choices to tonal context before Byte 4 taste.
- Implemented Byte 3c: note-on flash now uses Pixi-safe halo alpha headroom plus scale pulse, posture/listening share the same 8-beat window constant, and deterministic transport patterns materialize scale-degree notes from the world tonal context.
- Added `.agent/handoffs/2026-05-30-copy-paste-claude-byte-3c-review.md` so Arne can route the pushed Byte 3c follow-up to Claude Code for review.
- Received Claude's Byte 3c code review via Arne's manual relay and fetched the durable artifact from `claude/byte-3c-code-review` commit `2198e4b`. Byte 3c is approved; Byte 4 subjective taste is clear to start, with forward notes to re-materialize patterns if tonal context changes at runtime and to consider moving `TonalContext` into `src/tonal-context.ts`.
- Implemented Byte 4: added deterministic player taste profiles, inspectable taste evaluations, `window.taste.getEvaluations()`, basic listening loudness/brightness/energy metrics, and a transport note-decision path that can scale notes or emit structured rest events.
- Added `.agent/handoffs/2026-05-30-copy-paste-claude-byte-4-review.md` so Arne can route the pushed Byte 4 subjective-taste implementation to Claude Code for review.
- Received Claude's Byte 4 code review via Arne's manual relay and fetched the durable artifact from `claude/byte-4-code-review` commit `bccfb9a`. Byte 4 is approved; before Byte 5, add action dwell/hysteresis to stop melody rest/contrast threshold hunting.
- Implemented Byte 4b: taste evaluations now hold actions for a minimum beat dwell before switching, while still updating metrics and explanations; transport also passes scheduled snapshots through note-event emission instead of recomputing them.
- Received Claude's Byte 4b code review via Arne's manual relay and fetched the durable artifact from `claude/byte-4b-code-review` commit `f40af86`. Byte 4b is approved; Byte 5 lookahead is clear to start. Forward notes: dwell rate-limits but does not settle melody rest/contrast oscillation, the smoke assertion is timing-fragile, initial-evaluation detection should use an explicit marker, and the subtitle still says Byte 4.

## 2026-05-31

- Implemented Byte 5: deterministic material now enters an 8-beat lookahead queue of one-shot Tone transport events; the app exposes buffer health, lead beats, scheduled-through beat, and pending item count in both `window.transport.getState()` and the inspector.
- Updated the smoke test so `scheduledEventCount` means pending scheduled note/rest slots rather than three repeating sequences, and verified the lookahead queue remains bounded and returns to zero across rapid start/stop cycles.
- Added `.agent/handoffs/2026-05-31-copy-paste-claude-byte-5-review.md` so Arne can route the pushed Byte 5 lookahead implementation to Claude Code for review.
- Received Claude's Byte 5 code review via Arne's manual relay and fast-forwarded the durable artifact from `claude/byte-5-code-review` commit `a46de85`. Byte 5 is approved; Byte 6 session modes are clear to start. Forward notes: consolidate the duplicated scheduled-count naming, watch background-tab `setInterval` buffer drain, and remember that rest/velocity decisions are still made at fire time rather than at lookahead commit time.
- Cleaned up the Byte 5 lookahead naming before Byte 6: removed duplicate `scheduledEventCount`, renamed the canonical queue count to `lookahead.pendingSlotCount`, and changed visible labels to distinguish pending future slots from heard listening events.
- Implemented Byte 6a: added canonical session modes (`break`, `solo-practice`, `rehearsal`, `performance`), stored current mode in `GrowWorldState`, added top-bar segmented mode controls, inspector/status readouts, and `window.session` inspection hooks without changing playback behavior.
- Added `.agent/handoffs/2026-05-31-copy-paste-claude-byte-6a-review.md` so Arne can route the pushed Byte 6a session-mode shell to Claude Code for review.
- Received Claude's Byte 6a code review via Arne's manual relay and fast-forwarded the durable artifact from `claude/byte-6a-code-review` commit `e327326`. Byte 6a is approved; Byte 6b should start with break vs rehearsal only: break stops committing new lookahead slots and lets the queue drain, rehearsal resumes refill, solo-practice/performance stay no-ops.
- Implemented Byte 6b: transport now reads session mode, `break` stops future lookahead refill while committed slots drain, `rehearsal` resumes refill from the current beat, and `solo-practice`/`performance` remain rehearsal-equivalent no-ops for now.
- Added `.agent/handoffs/2026-05-31-copy-paste-claude-byte-6b-review.md` so Arne can route the pushed Byte 6b drain/resume behavior to Claude Code for review.
- Received Claude's Byte 6b code review via Arne's manual relay and fast-forwarded the durable artifact from `claude/byte-6b-code-review` commit `d65c7e5`. Byte 6b is approved; before adding more mode behavior, prefer a small Byte 6c cleanup that moves the mode-to-refill scheduling policy out of transport and into an explicit session-layer predicate or policy map.
- Implemented Byte 6c: added explicit session mode refill policies in `src/session-mode.ts`, changed transport to receive a narrow `shouldRefillLookahead` predicate, preserved `sessionMode` on transport state for display, and kept Byte 6b break/rehearsal behavior unchanged.
- Added `.agent/handoffs/2026-05-31-copy-paste-claude-byte-6c-review.md` so Arne can route the pushed Byte 6c policy-boundary cleanup to Claude Code for review.
- Updated the post-6c implementation plan from Arne's direction: player Ollama thinking moves before producer work. The next arc becomes player profiles/backstory fragments, deterministic thought-context selection, musical-excerpt markup, strict thought request/intent protocol with request levels, Ollama health/session primer, one slow-thinking player loop, song sketch/piece construction, and thought memory/persistence prep.
- Received Claude's Byte 6c code review via Arne's manual relay and preserved the durable artifact from `claude/byte-6c-code-review`. Byte 6c is approved; Claude verified the `SessionModePolicy` compile-time fail-safe and confirmed no Byte 6b behavior drift.
- Implemented Byte 7: added compact player thinking profiles with disposition traits and memory fragments, deterministic `PlayerThoughtSeed` assembly from listening/taste/motif context, a Thoughts inspector section, and `window.thinking.getSeeds()` for review before Ollama exists.
- Added `.agent/handoffs/2026-05-31-copy-paste-claude-byte-7-review.md` so Arne can route the pushed Byte 7 thought-seed implementation to Claude Code for review.
- Received Claude's Byte 7 code review via Arne's manual relay and fast-forwarded the durable artifact from `claude/byte-7-code-review` commit `8dd44b4`. Byte 7 is approved; Byte 8 should start by promoting motif excerpts into validatable phrase-relative `MusicalExcerpt` markup, then reconcile disposition/taste overlap and define the seed-to-request boundary.
- Implemented Byte 8: added the strict thought protocol module with phrase-relative `MusicalExcerpt` markup, `PlayerThoughtRequest`/`PlayerThoughtIntent` types, validators, deterministic mock intents, inspector summaries, and `window.thinking.getRequests()`/`getMockIntents()` hooks without calling Ollama or changing playback behavior.
- Added `.agent/handoffs/2026-05-31-copy-paste-claude-byte-8-review.md` so Arne can route the pushed Byte 8 protocol implementation to Claude Code for review.
- Received Claude's Byte 8 code review via Arne's manual relay and fast-forwarded the durable artifact from `claude/byte-8-code-review` commit `2be9d9d`. Byte 8 is approved; Byte 9 should tighten intent validation for in-scale and duration bounds before adding Ollama health/status, session primer, and manual validation display.
- Implemented Byte 9a: hardened thought validators to reject out-of-scale scale degrees, out-of-scale pitches, pitch/degree disagreement, and over-horizon musical ideas before any Ollama-authored intent is trusted.
- Added `.agent/handoffs/2026-05-31-copy-paste-claude-byte-9a-review.md` so Arne can route the pushed Byte 9a validator-hardening implementation to Claude Code for review.
- Received Claude's Byte 9a code review via Arne's manual relay and fast-forwarded the durable artifact from `claude/byte-9a-code-review` commit `21336cd`. Byte 9a is approved; Byte 9b should specify the excerpt scale-degree convention, surface validator errors in the manual display, keep deterministic mock fallback, and keep `sourceStartBeat`/placement system-owned.
- Implemented Byte 9b: added a browser-side local Ollama boundary, configurable health check, session primer, manual `melody` thought request, raw/parsed/validated inspector display, influence-probe prompt fixture, deterministic mock fallback, and a no-scheduling guard for model output.
- Added `.agent/handoffs/2026-05-31-copy-paste-claude-byte-9b-review.md` so Arne can route the pushed Byte 9b Ollama probe implementation to Claude Code for review.
- Received Claude's Byte 9b code review via Arne's manual relay and fast-forwarded the durable artifact from `claude/byte-9b-code-review` commit `41a4849`. Byte 9b is approved; real Mac Mini testing used `gemma4:26b`, returned invalid content after about 22 seconds, and verified the mock fallback and no-scheduling gate against a live model.
- Added `docs/principles/reproducible-aliveness.md` and revised the implementation plan so deterministic expressive life comes before automatic model-driven music: velocity modulation, performed offsets, physical-difficulty microtiming, agitation/contagion, and then backend/prompt tuning before the slow-thinking player loop.
- Implemented Byte 10b: added deterministic per-player velocity expression modulators, recorded applied expression snapshots on musical events, exposed latest expression state through `window.transport.getState().expression.latest`, and added a `Dynamics` inspector row without changing timing, pitch, lookahead, session modes, or Ollama behavior.
- Received Claude's Byte 10b code review via Arne's manual relay and fast-forwarded the durable artifact from `claude/byte-10b-code-review` commit `cba36d5`. Byte 10b is approved; Byte 10c should compute performed-offset data at schedule/commit time, keep `absoluteBeat` as grid truth, and choose a canonical per-player event index before audible microtiming.
- Implemented Byte 10c: added deterministic performed-offset data at lookahead commit time, moved the canonical per-player event index to schedule time, attached offset metadata to musical events, exposed latest timing snapshots in transport state, and rendered a debug-only `Offset` inspector row without audibly shifting playback.
- Received Claude's Byte 10c code review via Arne's manual relay and fast-forwarded the durable artifact from `claude/byte-10c-code-review` commit `9dfa8b5`. Byte 10c is approved; Byte 10d is ready, with forward notes to label just-heard versus queued debug surfaces, clamp audible offset fire times, and preserve transport generator state for future seek-and-continue checkpoints.
- Implemented Byte 10d: performed offsets now schedule Tone one-shots at shifted transport ticks, synth fire times clamp late callbacks safely, musical events carry `performedOffsetSeconds`, and the timing model now includes leap/register/density difficulty weighted by player disposition.
- Added smoke coverage for Byte 10d offset replay across transport restarts while preserving grid-stable `absoluteBeat` assertions and bounded lookahead cleanup.
- Ran a prompt-shape experiment for future Ollama tuning: full request JSON vs projected JSON vs music-card vs split-cards. Current recommendation is to move Byte 10f toward a trimmed projected request first, then live-test the smaller card protocols when local Ollama is reachable.
- Confirmed local Ollama reachability outside the Codex sandbox: `gemma4:31b` is available at `127.0.0.1:11434`. Live `/api/chat` tests showed `think: false` is needed for short JSON intents; projected JSON and music-card prompts parsed, while full JSON and split-cards hit the prediction limit.
- Pulled and tested `qwen3:4b-instruct-2507-q4_K_M` as the first fast local model candidate. With the same prompt-shape fixture and `think: false`, projected JSON returned valid output in about 2.7 seconds and music-card returned valid output in about 1.6 seconds; split-cards still hit the prediction limit.
- Pulled and tested `gemma3:4b-it-q4_K_M` as the second fast local model candidate. With the same fixture and settings, projected JSON returned valid output in about 2.3 seconds, full JSON and split-cards also parsed, and music-card was fast but omitted the required `rationale` field.
- Updated the plan for changing local model behavior: Byte 10f should add a prompt protocol registry and calibration harness while preserving one canonical `PlayerThoughtRequest`/`PlayerThoughtIntent` validator path. Protocols are model-facing adapters, not alternate musical contracts.
- Added a reusable inspector context-help shell with section-level info icons for Session, Ollama, Players, Thoughts, Listening, and Lookahead, so advanced controls can explain themselves as the app grows.
- Changed the context-help shell so the shared help panel moves inline under the section whose info icon was clicked, keeping help visible near the user's scroll position.
- Polished the main Grow UI layout while waiting on Claude review availability: header controls now share a stable control row, the terrarium canvas fills its available stage space with auto-focus scaling around the current players, and the inspector has a constrained draggable divider with keyboard and ARIA support.
- Rewrote `README.md` for the now-public GitHub repo, shifting it from a byte-by-byte changelog to a public-facing orientation around Grow's local AI music terrarium goal, current runnable state, Studio Pattern collaboration process, local model direction, and intentionally fluid product drift.
- Received Claude's Byte 10d code review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-10d-code-review` commit `e795204`. Byte 10d is approved; forward notes are to decide whether difficulty should push or drag by default, capture `latestCommittedPitchByPlayer` in future seek-and-continue checkpoint state, consolidate duplicated pitch parsers, and switch the next Ollama byte toward `qwen3:4b-instruct-2507-q4_K_M` plus schema-constrained projected JSON while keeping mock fallback.
- Implemented Byte 10e: the listening frame now exposes bounded mix agitation from timing variance, velocity spikes, density pressure, and push/drag pressure; each frame player carries disposition-shaped contagion; the inspector shows `Agitation` and per-player `Heat caught` without feeding those values into taste, transport, or scheduling decisions yet.
- Added `.agent/handoffs/2026-05-31-copy-paste-claude-byte-10e-review.md` so Arne can route the pushed Byte 10e agitation/contagion implementation to Claude Code for review.
- Received Claude's Byte 10e code review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-10e-code-review` commit `f62d520`. Byte 10e is approved; forward notes are to add a ceiling plus slow decay governor before contagion affects behavior, recognize that current agitation is density/velocity-led because microtiming is subtle, and consider using agitation/contagion as terrarium visual intensity before making it audible behavior.
- Implemented Byte 10e-v: the terrarium now paints a bounded room heat overlay from `ListeningFrame.mix.agitation`, raises player halo intensity/scale from per-player contagion, and exposes `window.terrarium.getVisualState()` for smoke tests while leaving audio, taste, transport, scheduling, and Ollama behavior unchanged.
- Received Claude's Byte 10e-v code review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-10e-v-code-review` commit `7f09dd3`. Byte 10e-v is approved; forward notes are that room warmth is currently subtle and can be raised if at-a-glance legibility matters, and that this visual layer is a useful tuning instrument for the future contagion build/release governor.
- Implemented Byte 10f-a: the manual Ollama thought probe now defaults to `qwen3:4b-instruct-2507-q4_K_M`, sends a compact `projected-json` prompt adapter instead of full `PlayerThoughtRequest` JSON, uses an Ollama JSON-schema `format`, surfaces the selected prompt protocol in the inspector, and preserves the canonical validator plus deterministic mock fallback without scheduling model output into music.
- Received Claude's Byte 10f-a code review and live qwen3 addendum via Arne's manual relay, then cherry-picked the durable artifacts from `claude/byte-10f-a-code-review` commits `766df58` and `0ab53cf`. Byte 10f-a is approved; the real `qwen3:4b-instruct-2507-q4_K_M` model returned valid intents for pulse, bass, and melody through projected JSON in about 4-5 seconds each. Next recommended byte is the local backend proxy before any automatic slow-thinking loop.
- Implemented Byte 10f-b1: added a Vite dev middleware local proxy for `/api/ollama/tags` and `/api/ollama/chat`, rewired health checks and manual thought probes through the same-origin proxy, restricted proxy targets to localhost Ollama URLs, and kept prompt construction, parsing, validation, mock fallback, and music scheduling behavior unchanged.
- Captured Arne's live-preview timing observation: the current Byte 10d audible microtiming can sound like perpetual stumble rather than ensemble tempo/groove. Updated the reproducible-aliveness principle and implementation plan with a future timing-feel retune: separate shared tempo drift, shared groove pocket, per-player pocket, material pressure, and rare stumble/recovery so most timing variation reads as pocket rather than error.
- Investigated Arne's follow-up that even `Grid` mode still sounded stumbly. The root cause was the final audio-fire clamp comparing scheduled transport callback time to `Tone.now()`, which includes Tone's lookahead and converted browser callback jitter into 3-60ms audible lateness. The clamp now uses `Tone.immediate()` plus a tiny epsilon, and `window.transport.getTimingDiagnostics()` plus smoke coverage verify future grid callbacks fire at their scheduled audio time when they are not truly late.
- Added a temporary timing audition mode, `Wide`, beside `Grid` and normal `Feel`. It magnifies the same deterministic pocket curve and tags events with `timing:wide-audition`; a live probe measured Grid at `0ms`, normal Feel around `7ms` max, and Wide around `26ms` max so Arne can recalibrate his ear against an obviously audible timing layer.
- Added a small `Song` material selector with `Lantern`, `Switchback`, and `Glass` deterministic loops so timing-feel auditions can start from fresh material instead of the same memorized pattern. Song switches refresh the lookahead, clear the current listening/taste frame, and tag future events with `song:<id>`.
- Captured Arne's DAW tempo-mapping heuristic in the timing plan: future groove work should use anchored bar downbeats plus eighth-note control points with interpolation, so timing feel is a coherent groove/tempo map that can repeat or morph rather than unrelated per-note offsets.
- Received Claude's Byte 10f-b1 code review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-10f-b1-code-review` commit `8458b6e`. Byte 10f-b1 is approved; forward notes are to remove model-authored `pitch` from the model-facing step schema and derive it from `scaleDegree` plus `octave`, add upstream abort propagation before the automatic slow-thinking loop, and re-host the Vite dev proxy as a standalone local server when persistence/backend work lands.
- Implemented Byte 10f-b2: the projected JSON schema no longer accepts model-authored `pitch`, the Ollama primer/prompt tells models to emit `scaleDegree` plus `octave`, `src/ollama.ts` derives canonical pitch before validation, the Vite proxy propagates browser aborts to upstream Ollama fetches, and smoke coverage now checks derived pitch plus proxy-failure mock fallback.
- Received Claude's Byte 10f-b2 code review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-10f-b2-code-review` commit `c86afe4`. Byte 10f-b2 is approved for merge; real qwen3 testing returned 6/6 valid manual thought tests. Follow up before Byte 11 with a malformed/invalid-200 smoke fixture, then start the slow-thinking loop rather than delaying for a model picker.
- Added the pre-Byte-11 invalid-200 Ollama smoke fixture requested by Claude: a mocked HTTP 200 response with parseable but validator-failing JSON now yields `invalid`, keeps provider `ollama`, preserves a valid deterministic mock fallback, and leaves transport stopped.
- Implemented Byte 11a: melody now has an automatic slow-thinking loop that runs only while transport is playing in rehearsal and Ollama health is ready, sends one validated request at a time, marks melody as `thinking` while pending, stores accepted/invalid/failed status in the Thoughts inspector and `window.thinking.getSlowLoop()`, retargets late valid thoughts to the next bar for inspection, and still does not schedule model output into audio.
- Received Claude's Byte 11a code review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-11a-code-review` commit `d3b2165`. Byte 11a is approved for merge; forward notes for Byte 11b are to define an explicit accepted-intent handoff, extract a slow-thinking controller before behavior grows, keep the first audio compiler narrow, re-check retargeting at schedule time with a thrash guard, and move expanded mode eligibility into session policies.
- Implemented Byte 11b: extracted the slow-thinking request lifecycle into `src/slow-thinking.ts`, added an explicit accepted-intent handoff, narrowed the automatic melody request to `rest`/`simplify`/`change_density`, and compiled accepted slow thoughts into a bounded future rest/thin playback window via the existing note-decision path.
- Added Byte 11b smoke coverage for the first audible model bridge: a mocked valid `rest` intent now produces `window.thinking.getSlowPlayback()` state and a melody `rest` event inside the compiled future window, while the prompt excludes non-compilable actions like `vary_motif`.
- Received Claude's Byte 11b code review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-11b-code-review` commit `3526f49`. Byte 11b is approved and merged; forward notes are to prune the unused accepted queue, move any future material-injecting action to the commit/lookahead path, use a per-player active-window map once a second player thinks, and fold expanded thinking eligibility into `SESSION_MODE_POLICIES`.
- Cleaned the Byte 11b slow-thinking handoff before new behavior: removed the unused accepted queue and pull API from `src/slow-thinking.ts`, leaving `onAccepted` as the single accepted-intent handoff path.
- Implemented Byte 11c-a: automatic melody slow-thinking can now accept `shift_register`; accepted register intents derive a bounded one-octave shift from source/target octave, expose it through `window.thinking.getSlowPlayback()`, and apply it as a pitch override to existing scheduled melody notes only.
- Added Byte 11c-a smoke coverage for the first pitch-changing slow thought: a mocked valid `shift_register` intent now produces melody note events shifted into octave 5 and tagged with `thought:shift_register` plus `register:+1`.
- Received Claude's Byte 11c-a code review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-11c-a-code-review` commit `ae215bf`. Byte 11c-a is approved and merged; forward notes are to name the slow-thought-over-taste rescue precedence, add an explicit `registerDelta`, and later record grid pitch versus performed pitch structurally for replay.
- Implemented Byte 11c-b cleanup: `shift_register` intents now use explicit `registerDelta` instead of inferred source/target octave differences. The primer, projected JSON schema, parser/coercer, validator, mock intent path, slow playback compiler, and smoke coverage all use the same bounded `-1 | 0 | 1` contract.
- Received Claude's Byte 11c-b code review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-11c-b-code-review` commit `ab71abf`. Byte 11c-b is approved and merged; the live qwen finding is that real `shift_register` intents currently omit `registerDelta` and are safely rejected to fallback. Follow up with schema-condition/example compliance work rather than restoring inference, and prune `AcceptedSlowThought.request` as dead state.
- Implemented Byte 11c-c: projected JSON now emits an `if action is shift_register then require registerDelta` schema condition, the prompt/primer include a compact top-level `shift_register` example, and wording explicitly tells the model not to hide `registerDelta` in rationale. A minimal live qwen probe showed conditional schema plus a plain example was insufficient, but the top-level/rationale warning made qwen emit `registerDelta: 1`.
- Received Claude's Byte 11c-c code review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-11c-c-code-review` commit `3d0f665`. Byte 11c-c is approved and merged; the conditional schema stays as self-documentation/future-proofing while prompt wording plus validator/fallback remain the real guard.
- Implemented Byte 11d: bass now joins melody as a slow-thinking player, with a shared one-request-at-a-time evaluator, a staggered first bass turn, per-player slow-thought playback windows, plural debug APIs, and bass limited to rest/thin-style actions until a bass-specific pitch/register design exists.
- Received Claude's Byte 11d code review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-11d-code-review` commit `52cd8b0`. Byte 11d is approved and merged; Claude's key forward live finding was that qwen can leak `registerDelta` into non-shift bass actions because the property still appears in the schema even when `shift_register` is not allowed.
- Implemented Byte 11e: projected JSON now omits the `registerDelta` schema property entirely unless `shift_register` is in `allowedActions`, so bass/rest-thin lanes cannot emit the field under `additionalProperties:false`; the validator remains the final guard for any stray value.
- Received Claude's Byte 11e sanity-check review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-11e-code-review` commit `868cfe9`. Byte 11e is approved and merged; the remaining optional pre-Byte-12 cleanup is to gate the static `registerDelta` prompt sentence on the same `allowsRegisterShift` condition.
- Implemented Byte 12a: added the first inspect-only band-level `SongSketch` model and inspector/debug surface, generated deterministically from selected song material, tonal context, and player roster. Also folded in the Byte 11e prompt cleanup by gating the `registerDelta` instruction sentence out of non-shift system/user prompts.
- Received Claude's Byte 12a code review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-12a-code-review` commit `7c7cbff`. Byte 12a is approved and merged; before 12b builds proposal/response on this shape, make the sketch song-material-deep rather than only using the selected song for id/label.
- Implemented Byte 12b-a: `SongSketch` now derives inspect-only chord/root plans and per-player density from `SongMaterial.patterns`. Chord plans are stored as roman-root numerals with root degrees preserved for structure, the inspector resolves those roots to current note names for readability, sketch content is cached by song/tonal/roster key, and smoke verifies Glass/Lantern differ structurally without changing playback, transport, slow-thinking, or validators.
- Received Claude's Byte 12b-a code review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-12b-a-code-review` commit `a7e1558`. Byte 12b-a is approved and merged; the next song-sketch work can build mock proposal/response on top of a genuinely per-song draft, with small forward notes around role-based harmonic source lookup, loop-length split consistency, and nested cached sketch immutability before mutation enters.
- Implemented Byte 12b-b: added a deterministic inspect-only `SongSketchProposal` with one response per player, rendered Proposal/Responses rows in the Song Sketch inspector, exposed `window.song.getProposal()`, resolved harmonic source lookup by bass role, and made `window.song.getSketch()` return cloned nested arrays instead of the cached base sketch. No playback, transport, slow-thinking, validators, Ollama, persistence, or scheduling behavior changed.
- Received Claude's Byte 12b-b code review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-12b-b-code-review` commit `2627382`. Byte 12b-b is approved and merged; next-slice steer is model-authored proposal text behind validator/mock fallback while keeping kind/stance/provenance deterministic. Persistence prep should come after that, and any proposal-to-playback bridge should be a later carefully gated byte.
- Implemented Byte 12b-c: added model-authored proposal wording as an inspect-only text overlay. Ollama can now manually rewrite proposal summary, requested action, and per-player response reasons through a bounded schema/validator, while proposal kind, target section, chord/root provenance, player ids, stances, routing, playback, transport, slow-thinking, and scheduling remain deterministic. Invalid or failed model text keeps the mock proposal active with a valid fallback.
- Received Claude's Byte 12b-c code review via Arne's manual relay and cherry-picked the durable artifact from `claude/byte-12b-c-code-review` commit `d723939`. Byte 12b-c is approved and merged; the key forward principle is that model-authored prose is data, not instruction, so any future proposal-to-playback bridge must act only on deterministic structured fields. Added a local-dev note that unrelated playback smoke tests with `eventCount: 0` can indicate a parked/wedged browser AudioContext, especially if the laptop display blanks; use `caffeinate -dimsu npm run smoke` for long unattended runs.
- Implemented Byte 13a: added `docs/persistence-records.md` to define durable record families for sessions, musical events, player thoughts, slow-thought playback windows, song sketches, deterministic proposals, model proposal text, checkpoints, and moments before adding SQLite or any write path. The prep records replay-vs-seek-and-continue boundaries, the future grid-vs-performed pitch gap, and the rule that model-authored prose is stored as data rather than executable instruction.
- Received Claude's Byte 13a code review via Arne's manual relay, merged `codex/byte-13a`, and cherry-picked the durable artifact from `claude/byte-13a-code-review` commit `f43d009`. Byte 13a is approved; Byte 13b should add the missing song/timing change record types, taste action-dwell generator state, and derived listening/agitation/contagion/taste display ephemeral guidance before the first SQLite writer lands.
- Implemented Byte 13b-a: added a dependency-free local SQLite shell using Node's `node:sqlite`, with `server/persistence.mjs` schema/init/append/read helpers, `scripts/grow-db.mjs` CLI commands, and `npm run db:init`, `npm run db:dump`, and `npm run db:smoke`. The byte also folds Claude's Byte 13a additions into the persistence docs while keeping the browser app, playback, transport, scheduling, replay, fork UI, and checkpoint restore unwired.
- Received Claude's Byte 13b-a code review via Arne's manual relay, applied the recommended `.gitignore` SQLite WAL/SHM sidecar fix plus a Node engine floor, merged `codex/byte-13b-a`, and cherry-picked the durable artifact from `claude/byte-13b-a-code-review` commit `62f73f4`. Byte 13b-a is approved; Byte 13b-b should wire low-frequency decision records (`session.started`, `session.mode_changed`, `song.changed`, `timing.feel_changed`) through a buffered off-audio-path writer, while holding `musical.event_recorded` for a separate high-frequency flush discipline slice.
- Implemented Byte 13b-b: added a browser-side persistence queue in `src/persistence.ts`, Vite dev middleware endpoints under `/api/persistence/*`, batch `appendEvents`, and app wiring for `session.started`, `session.mode_changed`, `song.changed`, and `timing.feel_changed`. The persistence schema now uses `beat` and `scheduled_beat` columns, and `musical.event_recorded` remains intentionally deferred until a high-frequency scheduler-safe writer slice.
- Received Claude's Byte 13b-b code review via Arne's manual relay, merged `codex/byte-13b-b`, and cherry-picked the durable artifact from `claude/byte-13b-b-code-review` commit `3f4c104`. Byte 13b-b is approved; next do Byte 13b-c1 writer hardening with read-only dump behavior, explicit unavailable/offline coverage, bounded retry/backoff, a pagehide best-effort flush, and a persistence inspector/debug line before the high-frequency `musical.event_recorded` byte.
- Implemented Byte 13b-c1: hardened the low-frequency persistence writer with read-only/no-db dump/status behavior, bounded retry/backoff, a `retrying` state, best-effort `flushOnPageHide()`, and a visible Session inspector persistence status line. Smoke now forces `/api/persistence/append` to fail and verifies the app remains usable with queued pending records; `musical.event_recorded` remains deferred.
- Received Claude's Byte 13b-c1 code review via Arne's manual relay and fast-forwarded the durable artifact from `claude/byte-13b-c1-code-review` commit `74dcb33`. Byte 13b-c1 is approved and merged; Byte 13b-c should persist `musical.event_recorded` through a scheduler-safe ring buffer plus batch flusher, with tests proving no fetch/timer work from scheduler callbacks, ordered/idempotent appends under load, explicit stop cleanup, and bounded back-pressure surfaced in the inspector.
- Implemented Byte 13b-c2: added explicit `gridPitch` and `performedPitch` to emitted `MusicalEvent` while preserving `pitch` as the performed-pitch compatibility field, introduced schema-v1 `MusicalEventRecordPayload` in `src/musical-event-record.ts`, and added a fixed-capacity drop-oldest musical-event record buffer. Tests cover grid/performed pitch payloads, buffer back-pressure/order, and live shift-register events carrying divergent grid/performed pitch.
- Received Claude's Byte 13b-c2 code review via Arne's manual relay and fast-forwarded the durable artifact from `claude/byte-13b-c2-code-review` commit `d4e29c5`. Byte 13b-c2 is approved and merged; Byte 13b-c3 should wire musical events into the buffer with a separate batch flusher, keep callback work to synchronous memory enqueue, route failed drained batches into the retained/idempotent persistence queue keyed by `sourceEventId`, and make stop cleanup plus buffer drops explicit.
- Implemented Byte 13b-c3: `handleMusicalEvent` now enqueues only the musical event plus a tonal-context snapshot into a source buffer, while a setup-time interval flusher builds `musical.event_recorded` payloads and hands deterministic session/source-event ids to the existing retained persistence queue. The source buffer is flushed on stop/pagehide/HMR, and the Session inspector exposes event-buffer pending/enqueued/dropped/last-flush state.
- Received Claude's Byte 13b-c3 code review via Arne's manual relay: architecture approved, but merge blocked because musical persistence ids collided across stop/start spans when transport-local `eventSerial` reset inside the same browser session. Implemented Byte 13b-c4 to add a play-span discriminator to musical persistence ids while keeping event ids span-local, and expanded smoke to prove two play spans persist every drained musical source event exactly once.
- Received Claude's Byte 13b-c4 code review via Arne's manual relay, merged `codex/byte-13b-c4` to `main` with Byte 13b-c3 included, and cherry-picked the durable artifact from `claude/byte-13b-c4-code-review` commit `a65ccaa`. Byte 13b-c4 is approved; next planned work pivots Byte 14 away from producer infrastructure and toward audible song form.
- Implemented Byte 14: added swappable song-form data and a looping arrangement timeline, committed a deterministic in-scale developed chorus melody through the lookahead material path, applied verse/chorus/bridge behavior through the existing note-decision hook, and added a synced section readout to the Session inspector/status line. A live probe confirmed the first chorus at beat 32 with `section:chorus` / `section:developed-chorus` melody events (`G5`, `C5`, `Bb5`).
- Received Claude's Byte 14 code review via Arne's manual relay: it passed listening review and is recommended for merge, with one feel-only bridge fix. Applied that fix on `codex/byte-14` by keeping the bridge melody's octave lift in committed song-form material and removing the second fire-time octave shift from the note-decision layer.
- Implemented Byte 15a: added deterministic melody scoring and repair for the chorus. Players now carry tiny influence phrase corpora, `src/melody-scoring.ts` scores raw/repaired chorus material for landing, monotony, and surprise-vs-target from each player's perspective, the repaired chorus is committed through the song-form/lookahead material path, the inspector exposes Raw/Repaired A/B plus score/critique/perspective readout, and Up/Down feedback persists `song.take_feedback` records while Down rejects a phrase key and repairs again.
- Received Claude's Byte 15a review via Arne's manual relay: substrate approved but merge required a feel fix because the scorer was octave-blind and could reward a repaired chorus that plunged several octaves. Applied the fix by bounding repair candidates to the phrase register and making leap, monotony-interval, and surprise-prior interval scoring use register-aware melodic position rather than scale degree alone.
- Received Claude's Byte 15a register-fix sanity check via Arne's manual relay: blocker closed, repaired chorus stays in the intended register, max adjacent jump is bounded, deterministic/in-scale commitments remain intact, and smoke stays green. Byte 15a is approved on `main`; next work is Byte 15b-a, a model critic that selects among validated deterministic candidates without emitting notes.
- Implemented Byte 15b-a: `MelodyRepairTake` now exposes a bounded candidate menu of raw, heuristic, and alternate chorus repairs; the local Ollama melody critic receives only scored candidate IDs and returns `selectedCandidateId` plus short prose. Valid selections switch the active repaired chorus candidate through the existing lookahead material path, while invalid/failed/stale output falls back to the deterministic scorer. Human Up/Down feedback now records the active candidate and whether it came from the model critic or deterministic scorer.
- Received Claude's Byte 15b-a review via Arne's manual relay: approved for merge. Claude verified the model critic is a pure selector over app-owned candidates, safety is enforced by schema enum, validator, and lookup, selected candidates use the existing chorus/lookahead path, staleness resets, and feedback persists selected-by source. Main forward finding: the mechanism is right, but the current candidate menu is too clustered to prove model taste; next slice should diversify candidate strategies and log model-pick versus deterministic-best score deltas.
- Implemented Byte 15b-b: diversified the app-owned chorus candidate menu into named deterministic strategies (`balanced-repair`, `lifted-hook`, `stepwise-hook`, `spacious-hook`, `energetic-hook`, `cadence-hook`, with `local-alternate` fallback), exposed strategy and score-delta fields to the critic/inspector, and recorded manual critic outcomes as `song.melody_critic_selection` so model picks can be compared against the local best over time.
- Received Claude's Byte 15b-b review via Arne's manual relay: approved for merge. Claude verified the menu is genuinely strategy-diverse, the safety boundary is unchanged, `song.melody_critic_selection` captures the model-vs-heuristic comparison, and a live qwen run picked `spacious-hook` with a grounded rationale. Next direction: multi-player consensus, rhythm-section pushback, remember-good, and continued mining of critic-selection logs.
- Implemented Byte 15c-a: added deterministic band consensus over the existing app-owned chorus candidate menu. A model critic now proposes a candidate id, then pulse, bass, and melody respond with accept/defer/push preferences before consensus selects the active audible candidate through the same lookahead material path. The inspector exposes consensus status and player responses, `window.melodyRepair.getConsensus()` returns the structured decision, and persistence now records the proposal separately from the consensus-selected candidate.
- Received Claude's Byte 15c-a review via Arne's manual relay, merged `codex/byte-15c-a` to `main`, and cherry-picked the durable artifact from `claude/byte-15c-a-code-review` commit `3d251ff`. Byte 15c-a is approved; Claude confirmed the model's unilateral power is reduced, spacious-hook can pass consensus, weak lifted-hook is not rubber-stamped, and the UI/persistence separate proposal from selected candidate. Next slice should make remember-good use the proposal/consensus/feedback trail.

## 2026-06-12

- Course adjustment from Arne after a Claude Cowork project review: stop deepening chorus take selection and move to musical motion. The Byte 16 arc becomes musicality first: 16a audible harmonic motion from the SongSketch chord plan, 16b band-proposed key/mode change (bridge modulation through the Byte 12 proposal shape plus Byte 15c consensus), 16c model-authored phrase as a validator-gated candidate, 16d mark-a-moment. The standalone remember-good byte is folded into the arc and retargeted at band-level outcomes. Recorded in `.agent/handoffs/2026-06-12-claude-musicality-course-adjustment.md`, `docs/vision-and-plan.md`, and `docs/implementation-plan.md`.
- Implemented Byte 16a: made the SongSketch root plan audible without model involvement. `src/song-form.ts` now derives gather/answer/bridge root plans from song material, exposes active harmonic context, and recolors pulse/bass material inside the existing C mixolydian tonal context at lookahead materialization time. The Session inspector shows the active harmony, while melody material remains validator-safe over the moving modal roots.
- During Byte 16a validation, headless Playwright audio could report `running` while the clock stayed parked. Added a wall-clock fallback for already-committed lookahead events so tests and persistence can observe the same scheduled material even when the test AudioContext stalls; Tone callbacks remain the normal audio path and clear their fallback timers.
- Received Claude's Byte 16a review via Arne's manual relay, merged `codex/byte-16a` to `main`, and cherry-picked the durable artifact from `claude/byte-16a-code-review` commit `7b693ac`. Byte 16a is approved; Claude confirmed the harmonic motion is audible, song-derived, lookahead-committed, duplicate-safe under the fallback, and in-scale. Next recommended slice before larger modulation work: make melody scoring chord-aware by feeding the moving section root into landing/cadence checks.
- Implemented Byte 16a-b: the deterministic chorus repair scorer now uses the chorus/answer root plan from `deriveSongSectionRootPlans()` instead of the song-wide root list. The repaired chorus is still deterministic and in-scale, but accented/final landing checks and repair candidates now target the same moving roots the accompaniment plays. The Melody Score inspector exposes the scoring roots.
- Received Claude's Byte 16a-b review via Arne's manual relay, merged `codex/byte-16a-b` to `main`, and cherry-picked the durable artifact from `claude/byte-16a-b-code-review` commit `acfbccf`. Byte 16a-b is approved; Claude confirmed the scorer matches Byte 16a's chorus harmony root indexing, important repaired notes are chord tones of the moving root, and candidate diversity/consensus still behave. Next options: verse/bridge chord-aware scoring, form-level scoring, or the human/remember-good loop.
- Implemented Byte 16a-c: added `src/form-scoring.ts` as an inspect-only deterministic score for the whole default song form. It materializes one form pass through the existing song-form path plus current chorus development, then grades harmonic motion, energy arc, melodic coherence, and cadence/arrival. The Form Score inspector and `window.formScore.getScore()` expose the result without changing playback.
- Received Claude's Byte 16a-c review via Arne's manual relay, merged `codex/byte-16a-c` to `main`, and cherry-picked the durable artifact from `claude/byte-16a-c-code-review` commit `7b86178`. Byte 16a-c is approved; Claude confirmed the form score is inspect-only, grounded in materialized notes, and discriminates across Lantern/Switchback/Glass. Next steer: extract the shared section-dynamics policy before or alongside the first audible variant chooser, and consider strengthening the weakened-chorus smoke to assert the total score drops.
- Implemented Byte 16a-d: extracted shared section-dynamics policy into `src/section-dynamics.ts` and routed both playback's `applySongSectionDecision()` and form scoring's energy estimate through it. The extraction preserves the existing section behavior, adds smoke coverage for the shared policy, and strengthens the weakened-chorus form-score assertion so total score must drop along with cadence. Validation: build, db:smoke, smoke 30/30, and diff-check are green; npm audit still reports the pre-existing Vite/esbuild advisories.
- Received Claude's Byte 16a-d review via Arne's manual relay, merged `codex/byte-16a-d` to `main`, and cherry-picked the durable artifact from `claude/byte-16a-d-code-review` commit `77cda32`. Byte 16a-d is approved; Claude confirmed the section-dynamics policy is now one pure source used by playback and form scoring, form scoring remains inert/inspect-only, and behavior is preserved. Next slice: audible variant chooser using app-owned deterministic in-scale variants, form score as the ground-truth ruler, and inspect-to-audible gating.
- Implemented Byte 16b-a: added deterministic audible form variants (`Classic Arc`, `Early Hook`, `Wide Return`) as app-owned arrangement plus section-dynamics data in `src/form-variants.ts`. The Form Score inspector now scores all variants, marks the current winner, and exposes a selector; the selected variant drives transport arrangement, harmonic context, section decisions, and form scoring through the normal lookahead path. Switching variants refreshes lookahead, clears stale slow-thinking playback windows, and records `song.form_variant_changed`. Validation: build, db:smoke, smoke 32/32, and diff-check are green; npm audit still reports the pre-existing Vite/esbuild advisories.
- Received Claude's Byte 16b-a review via Arne's manual relay, merged `codex/byte-16b-a` to `main`, and cherry-picked the durable artifact from `claude/byte-16b-a-code-review` commit `bd2f56b`. Byte 16b-a is approved; Claude confirmed the form score now drives a real human-selected audio path, Classic Arc preserves existing behavior by default, Early Hook reaches chorus at beat 16, and scoring/playback share the selected variant source. Forward notes: Early Hook and Wide Return currently tie at 0.938, so improve form-score variant discrimination before any auto-drive step; keep the winner shown but not auto-applied.
- Implemented Byte 16b-b: added a proportion/payoff subscore to the form score so form variants are judged on first chorus arrival, final chorus room, bridge breath, and chorus/verse balance. The Form Score inspector now displays the proportion subscore, and smoke asserts Wide Return scores above Early Hook on both total and proportion while playback/selector behavior remains unchanged. Validation: build, db:smoke, smoke 32/32, and diff-check are green; npm audit still reports the pre-existing Vite/esbuild advisories.
- Received Claude's Byte 16b-b review via Arne's manual relay, merged `codex/byte-16b-b` to `main`, and cherry-picked the durable artifact from `claude/byte-16b-b-code-review` commit `1c08d4c`. Byte 16b-b is approved; Claude confirmed the new proportion subscore breaks the Early Hook/Wide Return tie for real musical reasons without changing playback. Forward note: tighten `bandScore`'s over-max branch before goal-relative scoring depends on ratio metrics.
- Routed Claude's SongGoal planning artifact from `claude/song-goal-arc-plan` commit `f69c5b4` into `.agent/handoffs/2026-06-13-claude-song-goal-arc-plan.md` and summarized the Byte 17 SongGoal arc in `docs/implementation-plan.md`. Next recommended implementation slice is Byte 17a: `SongGoal` contract, validator/clamps, deterministic keyword interpreter, and inspector/debug harness with no playback drive.

## 2026-06-13

- Implemented Byte 17a: added `src/song-goal.ts` with the broad bounded `SongGoal` contract, closed tonic/mode/form/influence vocabularies, numeric clamps, deterministic keyword interpreter, stable ids, and brief generation. The app now has an inspect-only Song Goal panel plus `window.songGoal.*` harness; it intentionally does not drive tempo, key/mode, form, player behavior, playback, or persistence yet. Validation so far: build green and smoke 35/35.
- Received Claude's Byte 17a review via Arne's manual relay, merged `codex/byte-17a` to `main`, and cherry-picked the durable artifact from `claude/byte-17a-code-review` commit `49699ce`. Byte 17a is approved; Claude confirmed the trust boundary with adversarial prose and validation probes. Carry forward before/inside 17b: tighten deterministic keyword matching so short substrings such as `air` in `repair` or `run` in `crunch` cannot mis-fire once SongGoal begins driving setup.
- Implemented Byte 17b-a: tightened SongGoal keyword matching from substring checks to token/phrase matching, added a smoke regression for `repair`/`crunch` false positives, made `GrowWorldState` hold mutable active tonal context, threaded active tonal context and tempo through transport handlers, and routed display/root-name formatting through the active context. This is setup plumbing only; SongGoal still does not apply tempo, key/mode, form preference, playback, lookahead, or persistence changes.
- Received Claude's Byte 17b-a review via Arne's manual relay, merged `codex/byte-17b-a` to `main`, and cherry-picked the durable artifact from `claude/byte-17b-a-code-review` commit `a635e01`. Byte 17b-a is approved. Key 17b-b caution: apply tempo/key/form through `refreshLookaheadSchedule()` or stop/start boundaries, not by flipping active fields alone while playing; otherwise committed material can keep old setup while new material changes. Also clear wall-clock fallback timers during refresh for tidiness.
- Implemented Byte 17b-b: added an explicit Song Goal Apply setup action that uses only validated structured fields to set active tonal context, tempo, and form variant. Applying resets stale chorus/critic/slow-thinking state, clears the event ledger, refreshes lookahead scheduling, clears wall-clock fallback timers, and persists `song.goal_set` with the full bounded `SongGoal` plus previous/next setup snapshots. Smoke now proves a `G dorian`/75 BPM/Wide Return goal updates display, sketch/scoring context, transport state, persistence, and heard pitch classes without parsing prose as instruction.
- Received Claude's Byte 17b-b review via Arne's manual relay, merged `codex/byte-17b-b` to `main`, and cherry-picked the durable artifact from `claude/byte-17b-b-code-review` commit `592ee1b`. Byte 17b-b is approved; Claude confirmed live non-default key/mode application, mid-playback setup switch cleanup, in-scale heard events, and the `song.goal_set` persistence boundary. Carry forward: add a no-op guard on re-apply later, note that goal-driven form change is represented inside `song.goal_set`, and flat scale spelling is cosmetic.
- Implemented Byte 17c-a: added a goal-derived section dynamics overlay so an applied SongGoal's `energy` and `sectionEmphasis` nudge the existing section-dynamics profile used by both playback and form scoring. This keeps character drive deterministic and bounded: no model, no prose parsing, no new note generation, and no schema change. The Song Goal applied readout now shows energy and section emphasis, and smoke/browser checks prove a slow spacious Wide Return goal lifts chorus/bridge presence relative to verse through the shared profile.
- Received Claude's Byte 17c-a review via Arne's manual relay, merged `codex/byte-17c-a` to `main`, and cherry-picked the durable artifact from `claude/byte-17c-a-code-review` commit `dd108a4`. Byte 17c-a is approved; Claude confirmed the overlay scales velocity only, playback/form scoring share the same adjusted profile, no prose/model/material authority was introduced, and the envelope is bounded. Carry forward: consider surfacing derived multipliers in the Applied readout and add the 17b-b re-apply no-op guard later.
- Implemented Byte 17c-b: added a goal-derived taste profile overlay so applied `surpriseTarget` nudges novelty/repetition preferences and role-specific `dispositionBias` nudges density target/tolerance through the existing taste evaluator. The player registry stays unchanged; `GrowWorldState.syncTasteEvaluations()` receives a profile resolver, and `window.taste.getProfiles()` exposes base vs adjusted profiles for review. This remains deterministic and bounded: no model, no prose parsing, no direct action override, no note generation/removal, and no persistence schema change.
- Received Claude's Byte 17c-b review via Arne's manual relay, merged `codex/byte-17c-b` to `main`, and cherry-picked the durable artifact from `claude/byte-17c-b-code-review` commit `9b33e03`. Byte 17c-b is approved; Claude confirmed the nudges only move taste-profile thresholds, action selection/dwell/note paths remain unchanged, the player registry is not mutated, and `window.taste.getProfiles()` cleanly exposes base vs adjusted values. Next likely slice: curated `influenceHints` as sealed vocabulary mapped to bounded in-scale prior nudges.

## 2026-06-14

- Received Claude's evolutionary-composition plan via Arne's manual relay from `claude/evolutionary-composition-plan` commit `d2e083c`. The new backbone is a bounded candidate population in SQLite: generators write many song/phrase/groove/harmony/form candidates, scorers assign fitness, selection keeps elites, and capped purging removes the rest with audit events. Codex track is A: candidate store and evolution engine.
- Implemented Track A1 candidate-store shell on `codex/byte-a1-candidate-store`: added the bounded `Candidate` contract and phrase-genome validator in `src/candidate-store.ts`, bumped SQLite schema to v3 with a mutable `candidates` table, added write/query/score/retain/purge/cap helpers and `/api/persistence/candidates/*` dev routes, exposed candidate methods through `window.persistence`, and added append-only `candidate.created/scored/retained/purged` audit events. No audio, model, generator loop, or playback behavior changed. Validation: build green, db:smoke green, smoke 40/40.
- Received Claude's Track A1 review via Arne's manual relay, merged `codex/byte-a1-candidate-store` to `main`, and cherry-picked the durable artifact from `claude/codex-a1-candidate-store-code-review` commit `277ccf6` as local commit `6f30980`. Track A1 is approved; carry forward non-blocking notes that non-phrase genomes need per-kind typed validators when generators land, caps currently inspect at most 500 alive candidates per kind, and developed children should be new candidate ids with `parentId` rather than in-place genome mutation. Next track is A2: pure fitness aggregation over candidate scores.
- Implemented Track A2 on `codex/byte-a2-fitness-aggregation`: added `src/candidate-fitness.ts` with a pure weighted scalar aggregator, contribution breakdown, ignored-key reporting, missing-score penalty, and copy-only candidate fitness preview. Exposed the helper through `window.persistence` for inspection without DB writes, documented the tunable first weights, and added smoke coverage for bounded scores/weights and non-mutating preview. While validating, tightened the A1 middleware smoke to use a unique branch id so persisted local candidate rows cannot affect cap assertions. Validation: build green, db:smoke green, smoke 41/41, diff-check green; npm audit still reports the known Vite/esbuild advisories.
- Received Claude's Track A2 review via Arne's manual relay, merged `codex/byte-a2-fitness-aggregation` to `main`, and cherry-picked the durable artifact from `claude/codex-a2-fitness-aggregation-code-review` commit `778b748` as local commit `8852092`. Track A2 is approved; carry forward that fitness should be treated as meaningful only within a candidate kind for now, A3 must remain strictly per-kind, and A4 must deep-clone genomes because A2's preview copy is intentionally shallow.
- Implemented Track A3 on `codex/byte-a3-selection`: added per-kind `selectCandidates()` to the candidate store, with deterministic ordering by `fitness DESC, generation ASC, created_at ASC, id ASC`. Selection marks the top `eliteLimit` candidates as `elite`, purges overflow, appends existing `candidate.retained`/`candidate.purged` audit events with `reason: "selection"` and rank, and is exposed through `/api/persistence/candidates/select`, the browser persistence client, and `window.persistence.selectCandidates()`. No audio, model, playback, generator loop, or cross-kind selection behavior consumes it. Validation: build green, db:smoke green, smoke 42/42, diff-check green; npm audit still reports the known Vite/esbuild advisories.
- Received Claude's Track A3 review via Arne's manual relay, merged `codex/byte-a3-selection` to `main`, and cherry-picked the durable artifact from `claude/codex-a3-selection-code-review` commit `636c1af` as local commit `9f2de65`. Track A3 is approved and the A1+A2+A3 candidate-store engine is complete; carry forward that the selection loop is strict elitism and should get a diversity lever later to avoid premature convergence, while A4 should deep-clone genomes, create new ids with `parentId` and `generation + 1`, and never mutate elites in place.
- Implemented Track A4 on `codex/byte-a4-development-hook`: added `developCandidate()` to clone an elite same-branch phrase parent, deep-clone its genome, apply a closed bounded `phrase.nudge` mutation, and create a new `alive` child candidate with `parentId`, `generation + 1`, empty scores, fitness 0, and caller-provided or derived deterministic seed. Child creation reuses `candidate.created` with `reason: "development"`, `parentId`, and normalized mutation; repeated identical development returns the existing child rather than duplicating it. The hook is exposed through `/api/persistence/candidates/develop`, the persistence client, and `window.persistence.developCandidate()`, but no audio, model, playback, scoring loop, or automatic evolution loop consumes it. Validation: build green, db:smoke green, smoke 43/43, diff-check green; npm audit still reports the known Vite/esbuild advisories.
- Merged Track A4 and `claude/prosody-track-clean` into `main` as the prerequisite base for Track D1, then implemented Track D1 on `codex/byte-d1-candidate-cycle`: added `runCandidateCycle()` to compose B4 `produceProsodyCandidates()`, A1 store, A2 `aggregateCandidateFitness()`, A3 selection, and A4 development into one deterministic inspect-only generation. The browser exposes `window.persistence.runCandidateCycle()`, branch-specific candidate writes/scores are supported by the persistence client, and smoke proves produce/store/score/select/develop plus repeated identical idempotence. No audio, model, playback, or transport behavior consumes the cycle. Validation: build green, db:smoke green, smoke 58/58, diff-check green; npm audit still reports the known Vite/esbuild advisories.
- Received Claude's Track D1 review via Arne's manual relay, merged `codex/byte-d1-candidate-cycle` to `main`, and cherry-picked the durable artifact from `claude/codex-d1-candidate-cycle-code-review` commit `5898da2` as local commit `f309789`. Track D1 is approved; carry forward that children are developed but not scored yet, candidate ids are global and can collide across branches with the same seed, `readExistingSelection()` duplicates A3's tie-break comparator, and development should move from generic `phrase.nudge` to B2 prosody operators before D2 performs an elite.
- Implemented Track D1b on `codex/byte-d1b-prosody-development`: D1 cycle children now use deterministic B2 prosody operators (`reFoot`, `varyContour`, `alterCadence`, `shiftAnacrusis`) rather than generic `phrase.nudge`. A4's `developCandidate()` path now accepts a closed `phrase.replace` mutation carrying operator metadata plus a validated replacement `PlayerPatternSource`, so the server still owns same-branch elite checks, child lineage/id/status, and the `candidate.created` development audit. The byte remains inspect-only; no audio, model, playback, transport, or automatic loop consumes developed children. Validation: build green, db:smoke green, smoke 58/58, diff-check green; npm audit still reports the known Vite/esbuild advisories.
- Received Claude's Track D1b review via Arne's manual relay, merged `codex/byte-d1b-prosody-development` to `main`, and cherry-picked the durable artifact from `claude/codex-d1b-prosody-development-code-review` commit `588eac0` as local commit `54934f3`. Track D1b is approved; Claude accepted the dev-only inspect-only trust split where the app computes the B2 transform and the server validates/persists the replacement. Carry forward: if persistence leaves dev-only or operator provenance becomes trusted ground truth, extract B2 operators to a shared client/server module and have the server recompute rather than duplicate them; also handle the rare no-op fallback edge before relying on the loop unattended.
- Implemented Track D2 on `codex/byte-d2-elite-audition`: the app can now audition the current top elite phrase candidate by routing its validated `PlayerPatternSource` genome through the existing `melodyPhrasing` transport hook. `window.prosody.auditionEliteCandidate({ branchId, candidateId? })` selects the highest-fitness elite within a branch, refreshes the lookahead, and makes that phrase the active melody source; `window.prosody.clearCandidateAudition()` removes the override. This is the first audible bridge from candidate population to playback and remains reversible/debug-driven: no model, no new audio path, no automatic evolution loop, no persistence schema changes. Validation so far: build green, db:smoke green, smoke 59/59.
- Received Claude's Track D2 review via Arne's manual relay and fast-forwarded `codex/byte-d2-elite-audition` into local `main`. D2 is approved; Claude live-verified the auditioned elite phrase plays in-scale and clearing the audition restores the prior source. Claude also found a high-priority cross-cutting fitness bug: phrase candidates carried prosody keys while A2's default weights used melody/form keys, so all phrase fitness aggregated to 0 and A3 selected by tie-break instead of musical quality.
- Implemented Track D2b on `codex/byte-d2b-phrase-fitness`: `aggregateCandidateFitness()` now uses phrase/prosody weights (`richness`, `anacrusis`, `questionAnswer`, `anchorContrast`) when `kind: "phrase"` is supplied or the score object is clearly prosody-shaped, and D1 passes the candidate kind when scoring produced candidates. The D1 update tolerance now matches the six-decimal aggregate precision so provisional B4 fitness is replaced by A2 fitness. Smoke asserts phrase scores contribute, ignored keys are empty, candidate-cycle fitness varies, and elites rank above purged candidates.
- Received Claude's Track D2b review via Arne's manual relay and fast-forwarded `codex/byte-d2b-phrase-fitness` into local `main`. D2b is approved; Claude live-verified phrase fitness is now non-zero and quality-driven, with seed `4242` selecting elites `[0.79, 0.68]` over purged max `0.67`. The main pre-D3 gate is now scoring developed children so lineages can compete across generations instead of being purged at `fitness: 0`.
- Implemented Track D3 on `codex/byte-d3-iterating-loop`: `runCandidateCycle()` now scores developed children with `scoreProsody() -> aggregateCandidateFitness({ kind: "phrase" }) -> scoreCandidate()`, no-op development is skipped instead of aborting, and `runEvolution()` runs bounded deterministic multi-generation cycles with per-generation top/mean elite fitness summaries. The browser exposes `window.persistence.runEvolution()` for inspect-only debug use; no audio, transport, model, or audition behavior consumes the loop. An observed 4-generation run with seed `4242` improved top fitness `0.6317 -> 0.7945 -> 0.7945 -> 0.8199`.

## 2026-06-15

- Received Claude's Track D3 review via Arne's manual relay and fast-forwarded `codex/byte-d3-iterating-loop` into local `main`. D3 is approved; Claude reproduced the seed `4242` curve through 8 generations (`0.632 -> 0.795 -> 0.795 -> 0.820`, then held), confirmed child scoring, no-op skip behavior, deterministic fresh-store runs, and no audio/model/transport consumers. The newly observable findings are strict-elitism convergence by generation 4 and same-seed cross-branch collisions because candidate ids do not include `branchId`; next likely byte is a small diversity lever, with branch-scoped ids before parallel population/A-B work.
- Implemented Track D4 on `codex/byte-d4-diversity-reservoir`: added `src/candidate-diversity.ts` with pitch-content interestingness plus prosody-score novelty, extended candidate status/audit vocabulary with `reserved`, and made `runEvolution()` optionally use a diversity floor plus latent reservoir. D4 preserves the top fitness elite, greedily adds distance-filtered diverse elites, reserves high-interest/low-fitness candidates, and develops a deterministic fraction of children from the reserved stratum. Default-off remains D3-identical. Observed D4 runs: seed `4242` top fitness `0.6317 -> 0.7945 -> 0.7945 -> 0.8199`, elite mean distance about `0.40`, reservoir size `2`; seed `515151` top fitness `0.7718 -> 0.8067 -> 0.8222`, reservoir size `2`.
- Received Claude's Track D4 review via Arne's manual relay and fast-forwarded `codex/byte-d4-diversity-reservoir` into local `main`. D4 is approved; Claude live-verified default-off D3 identity, top-fitness elitism plus diverse elites, high-interest/low-fitness `reserved` candidates with audit records, reserved-parent breeding, and interestingness orthogonality. Carry forward: surface per-generation elite mean-pairwise distance in summaries, fix branch-scoped candidate ids before matched diversity-on/off A/B experiments, and then run longer A/B tests to see whether the reservoir beats strict elitism rather than only preserving stepping stones.
- Implemented branch-scoped candidate identity on `codex/byte-branch-scoped-candidate-id`: candidate writes now prefix explicit and generated ids with a deterministic branch hash while keeping the existing single-column primary key, parent ids are scoped to the same branch, and score/status updates reject cross-branch candidates. The browser client pre-scopes writes but the server independently scopes and validates, so same-seed runs on different branches produce distinct rows while same-seed same-branch reruns remain idempotent. Smoke now covers same-seed cross-branch isolation and matched diversity-on/off branches in one store; D4 per-generation summaries expose elite mean distance for future A/B tuning.
- Received Claude's branch-scoped candidate id review via Arne's manual relay and fast-forwarded `codex/byte-branch-scoped-candidate-id` into local `main`. The byte is approved; Claude verified same-seed cross-branch isolation, same-branch idempotence, explicit and generated id scoping, scoped parent lineage, cross-branch score/develop rejection, and matched diversity A/B in one store. Claude's first matched 10-generation A/B: seed `4242` strict plateaued at `0.8199` while diversity reached `0.8654` (`+0.0455`); seeds `11`, `777`, `30303`, and `91234` tied. Diversity-on was never worse across 5 seeds and strictly better on 1/5.
- Implemented Track D5 on `codex/byte-d5-evolving-performance`: raised bounded evolution runs to 500 generations, added `startGenerationIndex` so batch-stepped evolution does not replay generation zero, and exposed `window.prosody.performEvolvingElite()` / `stopEvolvingElite()` / `getEvolvingPerformance()`. The driver is explicit/default-off, evolves phrase candidates in timer batches, and re-auditions through the existing D2 melody override only when a strictly higher-fitness elite appears. Stop cancels the timer and clears the candidate audition override. The cycle now normalizes aggregate fitness to the four-decimal precision persisted by the server before comparing/scoring, closing a repeated-run rescoring bug. Validation: build green, smoke 68/68, db:smoke green, diff-check green; npm audit still reports the known Vite/esbuild advisories that require a breaking Vite 8 force-fix.
- Received Claude's Track D5 review via Arne's manual relay, fast-forwarded `codex/byte-d5-evolving-performance` into local `main`, and preserved the review in `.agent/reviews/2026-06-15-claude-track-d5-code-review.md`. D5 is approved as a milestone: Claude live-verified the band performing its evolving best, with heard fitness improving `0.7945 -> 0.8199 -> 0.859` over 50 generations, all swaps strictly upward, the final melody in-scale, and stop clearing the override cleanly.
- Implemented Byte UI-1 on `codex/byte-ui1-instrument-shell`: re-homed the dense inspector panels into a default-closed Inspect drawer while keeping the existing `player-inspector` and every existing panel/test id mounted in the DOM. The top shell now keeps transport, session/song/timing controls, transport status, and compact tempo/key readouts visible without opening the drawer; opening the drawer reveals the same Song Goal, Song Sketch, Melody/Form Score, Ollama, player, thought, listening, lookahead, and persistence panels. Smoke now has an explicit drawer contract test plus open-before-interact steps for UI-visible inspector tests. Validation: build green, smoke 69/69, db:smoke green, diff-check green; npm audit still reports the known Vite/esbuild dev-server advisories.
- Received Claude's Byte UI-1 review via Arne's manual relay, fast-forwarded `codex/byte-ui1-instrument-shell` into local `main`, and preserved the review in `.agent/reviews/2026-06-15-claude-byte-ui1-code-review.md`. UI-1 is approved; Claude live-verified the drawer is closed by default, panels remain mounted while visually collapsed, high-level controls/stage remain visible, and drawered controls become interactable after `inspect-toggle`. Next planned UI slice is UI-2: a real high-level written-to-evolving control, not another inspector reshuffle.
- Implemented Byte UI-2 on `codex/byte-ui2-control-dial`: added a top-bar `Line` slider that orchestrates the already-approved prosody/evolving layers. Dial values below `0.34` are `written` (`prosodyEnabled=false`, evolving stopped), values `0.34..0.67` are `speaking` (`setProsodyEnabled(true)`, evolving stopped), and values `>=0.68` start D5 evolving performance through `performEvolvingElite()` with deterministic per-song options (`branchId: "dial-<songId>"`, 50 generations, batch 2, count 5, eliteLimit 3, diversity enabled). Sliding back down stops evolving through `stopEvolvingElitePerformance()` and restores the lower regime. The change adds no transport/scoring/candidate internals and is covered by a browser smoke test that proves default written state, speaking prosody, evolving audition, and teardown. Validation: build green, smoke 70/70, db:smoke green, diff-check green; npm audit still reports the known Vite/esbuild dev-server advisories.
- Received Claude's Byte UI-2 review via Arne's manual relay and fast-forwarded `codex/byte-ui2-control-dial` into local `main`. UI-2 is approved; Claude live-verified the full written -> speaking -> evolving -> written cycle, in-scale playback, D5 fitness climbing to `0.863` with four swaps, no restart-on-drag churn, and clean teardown back to written eighth-note material. Carry forward: tidy the pre-existing `getAudition()` state-shape quirk later, and UI-3 should surface the SongGoal idea entry as the prompt front door.

## 2026-06-16

- Implemented Byte L0a on `codex/byte-l0a-grow-language`: added a pure `src/grow-language.ts` vocabulary bridge for the requested four modes (`Sunshine`/ionian, `Strut`/mixolydian, `Smoke`/dorian, `Bruise`/aeolian), degree-color helpers/tokens, inert CSS custom properties `--degree-1..--degree-7`, and pure unit tests. `MODE_INTERVALS` is now exported without changing its contents so the bridge can use the existing interval table as its single source. Noted a design/spec mismatch: current `main` already includes `lydian` and `phrygian` in `MODE_INTERVALS`/`SONG_GOAL_MODES`, but L0a intentionally leaves them undefined for the future vocabulary byte.
- Received Claude's Byte L0a review via Arne's manual relay and fast-forwarded `codex/byte-l0a-grow-language` into local `main`. L0a is approved. Implemented follow-up Byte L0a-b on `codex/byte-l0a-b-complete-realized-modes`: the Grow language bridge now names all engine-realized modes by adding `Helium`/lydian and `Scorch`/phrygian, preserves `Freefall`/locrian as undefined until the future behavior byte, and extends unit tests to prove six-mode round trips, source intervals, brightness order, and degree roles.
- Implemented Byte L1a on `codex/byte-l1a-anchor-phrase`: added a pure additive `src/anchor-phrase.ts` representation for melody-first anchor phrases. The model uses 1-based Grow-language anchor degrees, absolute-beat anchors, closed connector kernels (`fill`, `detour`, `approach`, `orbit`, `skip`), first-class segment gaps as breaths, and validation/normalization with clamp reporting. Chosen caps: 16 segments, 64 anchors total, 512 beats phrase length, 64 beats max anchor duration, 1/16 beat minimum duration. Added a dedicated `unit:anchor-phrase` script and unit tests; no playback, scoring, candidate-store, prosody, UI, tonal-context, or SongGoal path consumes it yet.
- Implemented Byte L1b on `codex/byte-l1b-anchor-render`: added `src/anchor-phrase-render.ts`, a pure melody-first renderer from `AnchorPhrase` to the existing flat `PlayerPatternSource` shape. Anchors render as notes with 1-based language degrees converted to 0-based engine `scaleDegree`; `fill`, `approach`, and `detour` render deterministic diatonic connector notes strictly between anchor end/start windows; `orbit` falls back to fill and `skip` renders no intermediates for now. Connector notes are capped at 16 per connector, gaps between segments remain `null` slots, and `window.anchorPhrase.renderDemo()` exposes a read-only demo render without auditioning or changing playback.

## 2026-06-17

- Received Claude's Byte L1b review via Arne's manual relay: L1b is approved, with the anchor renderer verified in-scale, deterministic, additive, and read-only. Implemented Byte L1c on `codex/byte-l1c-orbit-skip`, stacked on L1b while the L branches wait to land on `main`: `orbit` now renders local upper/lower neighbor decorations around the from-anchor degree, and `skip` now renders arpeggiated non-adjacent degree leaps instead of the L1b safe fallback. The demo phrase now exercises fill, detour, approach, orbit, skip, and a real inter-segment breath. No playback, transport, scoring, candidate-store, generator, or UI behavior consumes the renderer.
- Implemented Byte L0b on `codex/byte-l0b-language-skin`: applied the Grow language skin to the two musical mode readouts only. The top control key readout and Listening tonal context now show evocative mode names such as `C Strut`/`G Smoke`, keep the canonical classical mode in `data-mode-classical`, and expose the classical bridge in a title attribute. Added a static degree-color legend inside the collapsed Inspect drawer using the inert `--degree-1..7` tokens and `DEGREE_COLORS` roles. Scope is display-only: no playback, scoring, representation, SongGoal interpretation, or transport behavior change.
- Received Claude's Byte L1c review via Arne's manual relay: connector vocabulary is approved and complete, with one non-blocking note that orbit `reach` is currently adjacent-only. Implemented Byte L1d on `codex/byte-l1d-prosody-anchors`: `generateProsodicMelody()` now keeps its external `PlayerPatternSource` contract but internally builds a native `AnchorPhrase` through the exported `generateProsodicAnchorPhrase()` and renders it with `renderAnchorPhrase()`. The generated phrase has two segments, a real breath, pickup anchors, dominant question cadence, tonic answer cadence, and connector-based fill/approach motion. `window.anchorPhrase.fromProsody()` exposes the native artifact for inspection; candidate-store, scoring, evolution, playback, transport, and persistence consumers remain on the existing flat phrase contract.
- Received Claude's Byte L1d review via Arne's manual relay and fast-forwarded `codex/byte-l1d-prosody-anchors` into `main`. Implemented Byte L1e on `codex/byte-l1e-anchor-genome-store`: phrase candidates can now store native `anchor-phrase/v1` genomes, while legacy flat `PlayerPatternSource` genomes remain valid for old data and manual fixtures. Added `renderPhraseCandidateGenome()` as the shared adapter for scoring, diversity metrics, B2 development, D2 audition, and evolving performance; `produceProsodyCandidates()` now writes anchor genomes and scores their rendered flat pattern. The server independently normalizes/bounds the anchor phrase JSON without a SQLite schema change. Validation: build green, unit suites green, smoke 70/70, db:smoke green, diff-check green; npm audit still reports the known Vite/esbuild advisories.
- Addressed Claude's Byte L1e request-changes review: the candidate-cycle development path now compares native `anchor-phrase/v1` replacement genomes against the native parent before posting `phrase.replace`, so lossy flat-to-anchor conversion cannot send a native no-op to the server. Persistence HTTP errors now include the server response body, so the existing no-op tolerance recognizes `Candidate development mutation did not change the genome` instead of rethrowing opaque `HTTP 400`. Regression coverage updated the no-op development smoke fixture; targeted matched-diversity/evolving-performance smokes passed, and full smoke passed 70/70 twice.
- Received Claude's Byte L1e fix re-review via Arne's manual relay and fast-forwarded `codex/byte-l1e-anchor-genome-store` into `main` at `0be9235`. L1e is approved and Phase 1 is complete end to end: prosody now generates native anchors/connectors, renders them to notes, and the candidate store persists the native phrase genome while existing scoring/development/audition/evolution paths consume it through the shared render adapter. Carry forward remains the duplicated client/server anchor-phrase validator/normalizer before untrusted clients or trusted provenance.
- Implemented Byte L2 on `codex/byte-l2-readonly-graphical-editor`: clicking the melody player card opens a prominent default-closed read-only graphical phrase editor overlay outside the Inspect drawer. The editor renders the current song's `window.anchorPhrase.fromProsody()` native `AnchorPhrase` as an SVG roll with bar/beat grid, degree-colored anchors, labeled connector curves, and a visible breath band between segments; the header reuses Grow language mode naming with classical hover/title data. Close button, Escape, and backdrop click dismiss it without touching transport. Smoke adds coverage for melody-card reachability, overlay grammar elements, song-switch rerender, and unchanged stopped transport. Visual capture confirmed 11 anchors, 9 connectors, 1 breath, and transport still stopped.
- Received Claude's Byte L2 review via Arne's manual relay and fast-forwarded `codex/byte-l2-readonly-graphical-editor` into `main` at `3eabee4`. L2 is approved: Claude live-verified the overlay is read-only/UI-only, 1:1 with `fromProsody()`, melody-only and keyboard reachable, dismissable through Close/Escape/backdrop, focus-restoring, degree-colored, connector-labeled, breath-visible, and non-disruptive to the UI-1/UI-2 shell. Smoke is now 71/71 with the editor test.
- Implemented Byte L3a on `codex/byte-l3a-anchor-editing`: the melody phrase editor now has an explicit anchor edit mode seeded from the current prosody `AnchorPhrase`, with one testable mutation path (`window.phraseEditor.editAnchor(segmentIndex, anchorIndex, patch)`) for degree/octave/start/duration/dynamics edits. Edits are snapped/clamped through `src/anchor-phrase-edit.ts`, rendered back through `renderAnchorPhrase()`, and heard via a highest-precedence in-session `editorMelodyOverride` that clears on revert, close, song change, or entry into the evolving regime. UI gestures cover anchor drag for time/pitch, right-edge resize, and a dynamics slider; persistence and candidate audition remain separate. Validation: build green, phrase/language unit suites green, smoke 72/72, db:smoke green, diff-check green; npm audit still reports the known Vite/esbuild advisories.
- Implemented Byte L3b on `codex/byte-l3b-connector-editing`: the melody phrase editor now lets edit-mode users select a connector, change its kernel (`fill`, `detour`, `approach`, `orbit`, `skip`), and adjust audible knobs (`reach`, `density`, `bias`, `pull`, `skew`) through one testable mutation path, `window.phraseEditor.editConnector(segmentIndex, connectorIndex, patch)`. `src/anchor-phrase-edit.ts` clamps connector knobs, rejects unknown kernels without changing the phrase, leaves anchors/segment timing untouched, and reuses the existing `editorMelodyOverride` plus `refreshLookaheadSchedule()` path so connector changes are heard live and remain reversible on revert/close/song-change/evolving. Color remains accepted by the API but hidden in UI because rendering is still diatonic-only. Validation: build green, unit suites green, smoke 73/73, db:smoke green, diff-check green; npm audit still reports the known Vite/esbuild advisories.
- Received Claude's Byte L3b review via Arne's manual relay and fast-forwarded `codex/byte-l3b-connector-editing` into local `main` at `3cc5218`. L3b is approved: Claude live-verified fill-to-orbit connector edits, unchanged anchors/structure, active in-scale override playback, invalid-kernel rejection, revert cleanup, existing refresh path reuse, and evolving-disable behavior. Carry forward: connector `color` remains inert until a future chromatic-render byte, and editor persistence is still in-session only until L4.
- Implemented Byte L3c on `codex/byte-l3c-structural-editing`: the melody phrase editor now supports structural in-session phrase edits through one pure/tested model layer: `addAnchorToPhrase`, `removeAnchorFromPhrase`, `splitSegmentInPhrase`, and `joinSegmentsInPhrase`. The `window.phraseEditor` harness exposes `addAnchor`, `removeAnchor`, `splitSegment`, and `joinSegments`; UI affordances add `+ Anchor`, `Remove`, `Split here`, `Join breath`, Delete-to-remove, and double-click-empty-space-to-add. Every structural edit runs through `normalizeAnchorPhrase`, preserves `connectors.length === anchors.length - 1`, ordering/non-overlap/gap/cap invariants, and reuses the existing `editorMelodyOverride -> refreshLookaheadSchedule()` audible path with L3a/L3b revert/close/song/evolving cleanup. Validation: build green, six unit suites green, smoke 74/74, db:smoke green, diff-check green; npm audit still reports the known Vite/esbuild advisories.
- Received Claude's Byte L3c review via Arne's manual relay and fast-forwarded `codex/byte-l3c-structural-editing` into local `main` at `76df6e1`. L3c is approved; Claude live-verified add/split/join/remove counts and invariants, in-scale override playback, edge rejections, caps, existing refresh path reuse, reversibility, and evolving-disable behavior. The L3 editing arc is complete: anchors, connectors, and phrase structure are editable by hand, audible, reversible, in-session, melody-only, and always in key. Next planned arc is L4 persistence/authoring/idea catalog, but wait for Claude's kickoff before building because it re-touches persistence and evolution.
- Implemented Byte L4a on `codex/byte-l4a-save-edited-phrase`: the melody editor now has a Save idea action that turns the current edit-mode `AnchorPhrase` into a native `anchor-phrase/v1` phrase candidate, renders/scores it with the existing prosody scorer, and persists it through the existing candidate-store `writeCandidate()` path. Authored ideas use stable per-song branches (`editor-<songId>`), omit explicit ids so the candidate validator/content hash preserves same-phrase idempotence, and keep the live editor override untouched. Minimal UI feedback shows saved count/branch/last id; no catalog browse, schema, server, scorer, transport, or playback path changed. Validation: build green, six unit suites green, smoke 75/75 with a new save/idempotence/round-trip/evolution smoke, db:smoke green, diff-check green; npm audit still reports the known Vite/esbuild advisories.
- Received Claude's Byte L4a review via Arne's manual relay and fast-forwarded `codex/byte-l4a-save-edited-phrase` into local `main` at `5808876`. L4a is approved: Claude live-verified saved native anchor genomes, override round-trip identity, same-phrase idempotent dedup, strict branch filtering, and clean `runEvolution` cycles over the authored branch. Carry forward for L4b: `editor-<songId>` branches can contain both human-saved generation-0 ideas and evolved descendants/purged rows, so the catalog should present or filter that distinction deliberately.

## 2026-06-19

- Implemented Byte L4b on `codex/byte-l4b-idea-catalog`: the melody editor header now has a real per-song idea catalog backed by `listCandidates({ kind: "phrase", branchId: "editor-<songId>" })`. The catalog includes the generated prosody entry plus non-purged saved/evolved phrase candidates sorted by fitness, tags candidates as authored/evolved with status and fitness, renders the selected idea read-only in the existing roll, and keeps browsing side-effect-free. Explicit Preview reuses the existing `editorMelodyOverride -> refreshLookaheadSchedule()` path, and Edit this idea seeds the L3 working phrase from native `anchor-phrase/v1` genomes while legacy flat candidates remain view-only. No persistence endpoint, schema, scorer, server, transport, or scheduling path changed. Validation: build green, six unit suites green, smoke 76/76, db:smoke green, diff-check green; npm audit still reports the known Vite/esbuild advisories.
- Addressed Claude's L4b test-hermeticity finding: the L4a/L4b smoke tests no longer assume an empty persistent `editor-lantern` branch. The save test now records the initial non-purged branch contents, uses unique edit dynamics, asserts the saved candidate by id and repeat-save idempotence, and the catalog test selects a native visible candidate rather than assuming index 1 is always native in a reused store. Targeted L4 smokes and full `npm run smoke` now pass against a reused local DB.
