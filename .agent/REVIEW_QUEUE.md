# Review Queue

Use this file for known risks, open questions, and review focus. Keep entries short enough that a fresh reviewer will read them.

## Open Questions

- Should the GitHub repo be public or private?
- Should the local checkout move to `/Users/arnegleason/code/github.com/arnegleason/grow` before the first commit?
- Will the product need GitHub as a repository remote only, or also as an app/API integration?
- If an app/API integration is needed, should it use a GitHub App, OAuth app, or temporary local token?
- Confirm exact Ollama model tags per machine before automatic real-model work. Current known useful tags include MacBook `gemma4:31b`, Mac Mini `gemma4:26b`, and fast instruct candidate `qwen3:4b-instruct-2507-q4_K_M`.
- What should the first capture rolling window length be?
- Should first best-moment detection be heuristic-only, human-marked-only, or include an observer player?
- How should player resistance to producer requests be bounded so it feels alive without getting annoying?
- Should the terrarium keep ambient memory between sessions or start fresh by default?
- Should producer movement be keyboard-based, click-to-move, or another input model?
- What should the producer proxy's first visual identity be: dot with halo, ring, cursor-like marker, or another simple sign?
- What first lookahead target feels right: 4 bars, 8 bars, or about 20 seconds?
- What should the user see when the lookahead buffer runs thin: pause, visible rehearsing, fallback groove, or a mix?
- What should a saved piece contain first: motifs, role assignments, cue points, mode/key/tempo, or all of these?

## Initial Review Focus

- Confirm that no credentials are committed.
- Confirm project structure once the runtime stack is selected.
- Confirm least-privilege GitHub scopes before any GitHub integration work.
- Review `docs/vision-and-plan.md` for scope discipline before implementation begins.
- Review the capture/replay approach before media export work starts.
- Claude Code/Mac Mini first assignment: read `CLAUDE.md` and `.agent/handoffs/2026-05-30-claude-planning-review.md`, then produce critique, strengths, creative drift ideas, and concrete next-step recommendations.
- Review `docs/persistence-checkpoints.md` before implementation: validate SQLite/event-log/snapshot/fork strategy and identify what should stay JSON versus relational.
- Current Claude Code/Mac Mini assignment: use `.agent/handoffs/2026-05-30-claude-review-and-creative-drift.md` as the primary prompt.
- Current Codex implementation guidance from Claude review: start with the smallest rule-based playable terrarium before adding Ollama, SQLite, producer avatar, or fork/capture UI.
- Time-model guidance from Arne: Grow can use a delayed-now/lookahead buffer; players may think non-real-time and commit upcoming material rather than producing every audible moment on the sample clock.
- Session-mode guidance from Arne: Grow is not a nonstop ambience generator; it should support breaks, solo practice, rehearsals, bounded performances, reflection, constructed pieces, and meaningful silence.
- Producer-proxy guidance from Arne: the human should be able to type natural-language prompts, and an interpreter should carry them into the world as movement, speech, cues, and negotiated requests rather than requiring structured controls.
- Scope guidance from Arne: keep Grow solo for now, but leave a future hook for multiple terrariums/bands that can observe, inspire, or react to each other.
- Claude reviewed `docs/implementation-plan.md`; current Byte 1 guidance is stationary pulse player + percussive beat + explicit Tone.js lifecycle cleanup before adding movement or more players.
- First implementation review should pay special attention to repeated start/stop cycles and whether scheduled Tone.js objects are disposed rather than stacked.
- Byte 6c is approved. Forward note: when richer mode behavior arrives, grow `SessionModePolicy` explicitly; if it gains several fields, consider passing the whole policy object rather than one transport handler per policy field.
- Planning review focus after Byte 9b: deterministic aliveness now precedes automatic model-driven music. Review whether the next arc is small enough: reproducible-aliveness principle -> velocity modulation -> performed offset model -> audible microtiming/physical difficulty -> agitation/contagion -> backend/prompt tuning -> one slow-thinking player.
- Byte 6b feel note: posture lags the audible silence by up to the 8-beat recent-activity window during a sustained break. This is correct now; revisit only if the break should read visually faster.
- Byte 6b smoke waits through an 8-beat drain and is intentionally longer. If it gets flaky, prefer probing latest recorded beat/dwell gaps over shortening the behavioral window.
- Byte 5 naming cleanup is implemented. Future review should check that `lookahead.pendingSlotCount`, visible `Pending`, and listening `Heard` labels stay distinct as Byte 6 adds more state.
- Byte 5 forward risk to keep visible: the lookahead refill uses wall-clock `setInterval`, so background tabs may drain the queue safely but drop newly scheduled notes until foregrounded.
- Byte 5 commitment boundary to keep visible: pitch/timing are committed into the lookahead queue, while rest/velocity taste decisions still happen at fire time.
- Byte 9a is approved. Claude verified validator hardening catches model-like bad output: `scaleDegree >= scale.length`, out-of-scale pitch classes, pitch/degree disagreement, and `musicalIdea.durationBeats > maxDurationBeats`.
- Byte 9b is approved. Claude verified a real Mac Mini `gemma4:26b` call safely returned invalid/empty content after about 22 seconds, with parse/validation errors visible and deterministic mock fallback valid.
- Byte 10a review focus: the reproducible-aliveness principle should clearly protect replayability while making room for deterministic heat, expressive mistakes, and bounded micro-variation.
- Byte 10b is approved. Claude verified deterministic replay across two runs, bounded role ranges, audible variation, unchanged grid timing, clean rests, and 4/4 smoke.
- Byte 10c is approved. Claude verified the shared schedule-time event index, deterministic bounded offsets, data-only playback, clean break/drain behavior, and 5/5 smoke.
- Byte 10d is approved. Claude verified audible offsets are scheduled off-grid while `absoluteBeat` stays grid truth, no per-player reorder occurs, offsets replay deterministically, break drain remains clean, and lifecycle stress is leak-free.
- Forward notes after Byte 10d: decide whether difficult material should baseline push/rush or drag/carefully slow, label `Dynamics` as just-heard versus `Offset` as next-committed, capture `latestCommittedPitchByPlayer` in future seek-and-continue generator state, and consolidate duplicated pitch parsers into a shared music-theory utility.
- Arne live-preview timing note: current audible microtiming can read as perpetual stumble. Hypothesis to review before more timing behavior: add a hierarchy of shared tempo drift, shared groove pocket, per-player pocket, material pressure, and rare stumble/recovery, rather than driving most feel from per-note offsets.
- Byte 10e is approved. Claude verified agitation/contagion is bounded, inspectable, read-only, playback-neutral, and genuinely disposition-differentiated: steady pulse damps heat while melody catches/amplifies it.
- Forward notes after Byte 10e: before contagion drives behavior, add a build/release governor with ceiling plus slow decay for the closed loop; current agitation is density/velocity-led because microtiming is subtle; agitation and contagion are natural terrarium visual-intensity signals before they become audible behavior.
- Byte 10f-a is approved. Live Mac Mini qwen3 verification returned valid projected-JSON intents for pulse, bass, and melody in about 4-5 seconds each, with clean parsing, zero validation errors, system-owned `sourceStartBeat`, and character-grounded rationales.
- Byte 10f-b1 is approved. Claude verified the Vite proxy is transport-only, localhost-scoped, and that browser code uses same-origin `/api/ollama/*` instead of direct Ollama fetches.
- Byte 10f-b2 is approved. Claude verified pitch is removed from the model schema, system-side pitch derivation does not mask bad degrees, proxy abort propagation works, and real qwen3 returned 6/6 valid manual thought tests.
- Pre-Byte-11 invalid-200 smoke fixture is implemented. A mocked HTTP 200 with validator-failing JSON yields `invalid`, provider `ollama`, valid mock fallback, and stopped transport.
- Byte 11a is approved. Claude verified no surprise calls before health check, playing+rehearsal+ready gating, one request at a time, thinking posture cleanup, non-blocking transport, clean discard on gate loss/stop, and real qwen3 pending-to-accepted cycles.
- Byte 11b is approved and merged. Claude verified the first audible bridge is bounded, melody-only, bar-snapped, rest/thin only, validator/fallback-gated, lifecycle-clean, live-qwen3 compatible, and smoke-covered against accepted-but-inert output.
- Byte 11b accepted-queue cleanup is implemented: `src/slow-thinking.ts` now has one accepted-intent handoff path, `onAccepted`.
- Byte 11c-a is approved and merged. Claude verified the first pitch-changing slow thought stays inside the safety envelope: melody-only, +/-1 octave, <=4-beat bar-snapped window, only existing scheduled notes, pulse/bass untouched, no slot changes, smoke 15/15, and live qwen3 compiling bounded `shift_register` windows.
- Byte 11c-a forward notes: the rescue behavior is acceptable but should be named as slow-thought precedence over taste rests; add explicit `registerDelta` so the model can state direction and choose no-op; when replay/event logging lands, record grid pitch versus performed pitch structurally.
- Byte 11c-b is approved and merged. Claude verified explicit `registerDelta` resolves the 11c-a implicitness note, strict validator coverage is complete, `registerDelta: 0` remains valid restraint, and the old `AcceptedSlowThought.request` should be pruned.
- Byte 11c-b live finding: real qwen currently selects `shift_register` but omits `registerDelta`, so the valid strict path rejects to deterministic fallback. Next compliance slice should try conditional structured-output schema support and/or a compact concrete example; do not reintroduce inference/defaulting.
- Byte 11c-c is approved and merged. Claude verified live qwen3 compliance improved to accepted `shift_register` intents with valid `registerDelta`; keep treating prompt wording plus validator/fallback as the durable guard because the schema conditional is mostly self-documenting with current local grammar support.
- Byte 11d is approved and merged. Claude verified two controllers, per-player playback windows, global one-pending serialization, lifecycle cleanup across both lanes, and smoke 16/16.
- Byte 11e is approved and merged. Claude confirmed bass/non-shift formats omit `registerDelta`/`shift_register`, melody shift formats still include the property and conditional, validator behavior is unchanged, and no playback/scheduling behavior moved.
- Byte 12a is approved and merged. Claude verified the inspect-only `SongSketch` is pure, band-level, playback-neutral, and that the folded prompt cleanup removes `registerDelta` from non-shift system/user/schema while preserving melody shift guidance.
- Byte 12b-a is approved and merged. Claude confirmed the roman-root `chordPlan` plus structured `rootDegrees` split, per-song density/root derivation, memoized content boundary, and inspect-only/no-playback line.
- Byte 12b-b is approved and merged. Claude confirmed the deterministic proposal/response surface is the right first band-level coordination shape and remains strictly inspect-only.
- Next proposal review focus: model-authored proposal text should use the existing validator + deterministic mock fallback, while kind/stance/provenance stay deterministic; do not fold persistence or proposal-to-playback into that small slice.
- Byte 12b-c is approved and merged. Claude verified the model is only a proposal-text copywriter, invalid/failed/stale output falls back to deterministic mock text, the model-facing schema omits structural fields, and playback/transport/slow-thinking/scheduling are untouched.
- Carry-forward principle from Byte 12b-c: model-authored prose is data, not instruction. When proposal-to-playback eventually lands, act only on deterministic structured fields such as kind, stance, chord/root plan, target section, and bounded validation output; never parse model prose as commands.
- Byte 13b-c1 is approved and merged. Claude's only minor notes were cosmetic/recovery polish: bfcache restore after pagehide can leave the visible status at `flushing` until the next record, transient retrying shows `lastError`, and `nextRetryAt` is tracked but not displayed.
- Byte 13b-c should persist `musical.event_recorded` behind a preallocated/high-frequency ring buffer plus separate timer/idle batch flusher. Review focus: no fetch/timer work inside Tone scheduler callbacks, ordering/seq under load, deliberate stop/cleanup flush-or-discard behavior, bounded back-pressure such as drop-oldest or coalesce surfaced in the inspector, and whether grid-vs-performed pitch split lands before or alongside the event record.
- Byte 13b-c2 is approved and merged. Claude confirmed the grid/performed schema is durable, `MusicalEvent.pitch` as performed alias is right for now, and drop-oldest is acceptable if replay treats drops as explicit discontinuities.
- Byte 13b-c4 is approved and merged. Claude confirmed the play-span id discriminator fixes the cross-span idempotency collision at the right layer. Carry forward: drops still have no persisted gap marker, and the old Byte 13b-c2 `MusicalEventRecordBuffer` is now dead code and can be removed with its test when convenient.
- Byte 14 passed Claude's listening review. One feel-only bridge fix was applied after review: the bridge melody lift now happens only in committed song-form material, not again in the fire-time note-decision layer. Recommended next step is a tiny sanity check or merge if Arne is happy by ear.
- Byte 15a is approved after Claude's register-fix sanity check and is on `main`: repair candidates are bounded to the phrase register and leap/surprise interval scoring is register-aware. Byte 15b-a should keep the model critic to selecting among scorer-validated deterministic candidates, not emitting notes.
- Byte 15b-a is approved and merged. Claude confirmed the model critic cannot emit or mutate notes, pitches, scores, timing, or section structure; valid output only selects a local candidate id; invalid/failed output leaves the deterministic scorer active; and a model-selected alternate changes the audible chorus candidate through the lookahead material path.
- Byte 15b-b is approved and merged. Claude confirmed the strategy-diverse menu gives the critic meaningful choices and that the safety boundary remains id-plus-prose only. Carry forward one non-blocking feel note: `lifted-hook` leans repetitive/high and can be tuned later if it proves unattractive rather than usefully discriminable.
- Byte 15c-a is approved and merged. Claude confirmed the model still only proposes an app-owned candidate id plus prose, deterministic band consensus can accept `spacious-hook` while rejecting a weak `lifted-hook`, invalid model output falls back to deterministic consensus, and UI/persistence cleanly separate proposal from consensus-selected candidate.
- Next Byte 15 direction: remember-good should use the stored proposal/consensus/feedback trail rather than only nudging a generic surprise weight. Also consider deriving consensus strategy affinities from player dispositions so scoring taste and consensus taste share one source of truth.
- 2026-06-12 course adjustment: the next arc is musicality first (Byte 16a-d in `docs/implementation-plan.md`), not another chorus-selection byte. Review focus for 16a: chord changes must commit through the lookahead material path with clean section-boundary timing and no model involvement. For 16b: modulation must reuse SongSketchProposal + 15c consensus, with stay-in-key as the always-valid fallback, and fold in disposition-derived consensus affinities. For 16c: validator + scorer + consensus must demonstrably contain a bad model-authored phrase before a good one is allowed to win.
- Byte 16a review focus: confirm pulse/bass harmonic motion is committed through `arrangeSongFormPatternEvent()` before pitch materialization, not through fire-time taste decisions; confirm the active scale/key stays C mixolydian for this byte; confirm verse/chorus/bridge roots are audibly distinct and lifecycle-safe across start/stop and song switches.
- Byte 16a transport fallback review focus: confirm wall-clock fallback timers for committed events do not double-emit in normal audio, are cleared on Tone callback/stop/dispose, and do not become a second scheduling authority.
- Byte 16a-b review focus: confirm melody scoring/repair follows the chorus `answer` root plan, not `deriveSongRootDegrees()`; confirm the repaired chorus still has useful candidate diversity and does not overfit into only chord tones.
- Byte 16a-c review focus: confirm form scoring is inspect-only and grounded in materialized form notes; confirm the energy/cadence/coherence subscores are useful enough to guide a later audible variant rather than overfitted to the current Lantern form.
- Byte 16a-d review focus: confirm `applySongSectionDecision()` and form scoring both use the shared `applySectionDynamics()` policy, with no audible behavior drift from the Byte 14/16a section decisions. In particular, chorus support players should preserve their incoming taste action, while chorus melody and bridge lanes still override as before.
- Byte 16b-a review focus: confirm Form Score's variant selector is a real audio path, not decorative UI. The selected variant should provide the transport arrangement and section-dynamics profile, default to Classic Arc, refresh lookahead on change, and keep all pitch material app-owned/in-scale. Verify Early Hook reaches chorus at beat 16 while Classic Arc is still verse there.
- Byte 16b-b review focus: confirm the new proportion/payoff subscore separates Early Hook and Wide Return without changing playback. The metric should be grounded in section proportions (first chorus arrival, final chorus room, bridge breath, chorus/verse balance), and the displayed winner should remain informational only.
- Byte 17b-b is approved and merged. Claude confirmed explicit apply semantics, seam-free refresh during key/tempo/form changes, in-scale non-default tonal contexts, and `song.goal_set` as the reproducible structured setup boundary. Carry forward non-blocking polish: no-op guard on re-apply, goal-driven form changes do not emit separate `song.form_variant_changed`, and scales use flat spelling.
- Byte 17c-a is approved and merged. Claude confirmed applied SongGoal `energy` and `sectionEmphasis` are only bounded velocity multipliers over the shared section-dynamics profile, with playback and form scoring sharing the same adjusted profile. Carry forward: optionally surface derived per-section multipliers in the Applied readout; default goal is a small nudge rather than a no-op; re-apply no-op guard still stands.
- Byte 17c-b is approved and merged. Claude confirmed applied SongGoal `surpriseTarget` and `dispositionBias` only derive adjusted taste-profile thresholds consumed by the existing taste evaluator; action choice, dwell, note paths, registry taste, prose/model behavior, and persistence schema stay untouched. Carry forward: readout shows inputs rather than derived thresholds, densityTolerance only widens, and the default goal is a small bounded nudge.
- Byte 17c-c review focus: curated `influenceHints` should remain a sealed vocabulary mapped to bounded in-scale prior nudges. Do not parse hint prose, accept freeform influence text, emit new notes, or create a new material source. Prefer adjusting existing melody-scoring/player prior inputs and exposing base vs adjusted prior effects for review.
- Track A1 is approved and merged. Carry forward Claude's non-blocking notes: add per-kind typed validators for non-phrase genomes when their generators land, keep candidate caps within the current <=500 alive-per-kind query window or paginate before exceeding it, and treat developed children as new ids with `parentId` rather than in-place genome mutation.
- Track A2 is approved and merged. Carry forward Claude's non-blocking notes: current scalar fitness is meaningful only within a candidate kind because missing weighted keys are a constant offset by kind, and `previewCandidateFitness()` is a shallow copy so A4 must deep-clone genomes before mutation.
- Track A3 is approved and merged; A1+A2+A3 candidate-store engine is complete. Carry forward: the loop is strict elitism, so add a diversity lever later before long-running evolution to avoid premature convergence; `MAX_CANDIDATE_LIMIT = 500` still bounds the ranked view.
- Track D1 is approved and merged. Carry forward before the first audible D2 payoff: developed children currently remain unscored with `fitness: 0`, so a true iterating loop would purge them; candidate ids omit `branchId`, so same-seed cycles collide across branches until ids or schema become branch-scoped; `readExistingSelection()` duplicates A3's rank comparator; and development should move from generic A4 `phrase.nudge` to B2 prosody operators (`varyContour`, `reFoot`, `alterCadence`, `shiftAnacrusis`) before performing elites.
- Track D1b is approved and merged. Carry forward: the dev-only trust split is acceptable while inspect-only, but before untrusted clients or trusted operator provenance, extract B2 operators into a shared client/server module so the server recomputes `phrase.replace` rather than trusting metadata. Also handle/skip the rare no-op operator fallback, score developed children before the next true selection, and fix branch-scoped candidate ids before parallel populations. Next planned payoff: D2 performs/auditions the current elite phrase candidate through the existing melody phrasing handler.
- Track D2b is approved and merged locally. Before D3 or any iterative loop, score developed children so lineages can compete instead of being purged at `fitness: 0`; the D2b fitness-key fix makes this newly load-bearing.
- Track D3 is approved and merged. Claude empirically confirmed the next two evolutionary-engine risks: strict elitism converges early (`0.632 -> 0.795 -> 0.795 -> 0.820`, then plateau) and same-seed cross-branch runs collide because candidate ids omit `branchId`.
- Track D4 is approved and merged. Claude verified default-off D3 identity, top-fitness elitism, diverse elites, high-interest/low-fitness `reserved` candidates, reserved-parent breeding, and interestingness orthogonality. Carry forward one reporting gap: per-generation summaries should expose elite mean-pairwise distance, not only `reservedCount`, so diversity can be tuned over time.
- Branch-scoped candidate ids are approved and merged. Claude verified the single-column PK plus `b<hash(branchId)>:` prefix mechanism and ran the first matched A/B: diversity-on was never worse across 5 seeds and beat strict elitism on seed `4242` (`0.8654` vs `0.8199` after 10 generations).
- Next candidate-engine experiment: run longer/more-seed matched diversity A/B with more exploration pressure, such as larger `reservoirLimit`, lower `minDistance`, or higher `reservoirParentFraction`, while keeping the knobs bounded and measuring top fitness plus elite mean distance.
- Track D5 review focus: `window.prosody.performEvolvingElite()` should be default-off, explicit, and timer-batched; it must audition only validated phrase elites through the existing D2 path (`applyCandidateMelodyAudition()` -> `refreshLookaheadSchedule()`), swap only on strictly higher fitness, and `stopEvolvingElite()` must cancel the run plus clear the melody override. Live review should confirm the heard melody changes as fitter elites appear while playing.
- Track D5 smoke note: candidate-cycle idempotence now asserts repeated identical runs append no additional audit events, rather than assuming every first-run `candidate.scored` event has a unique candidate id. This keeps the idempotence invariant while allowing legitimate first-run score history.
- Track D5 is approved and merged locally. Carry forward Claude's non-blocking observation: each swap triggers `refreshLookaheadSchedule()` with ledger clear and re-anchor; fine at current swap rates, but frequent swaps would mean frequent re-anchoring.
- Byte UI-1 is approved and merged locally. Claude confirmed the Inspect drawer is a visual collapse only, all sampled drawered test ids remain attached while closed, detail controls become interactable after `inspect-toggle`, high-level controls/stage stay visible, and no transport/scoring/evolution/persistence/window harness behavior changed.
- Byte UI-2 is approved and merged locally. Claude confirmed the top-bar written/speaking/evolving dial is orchestration-only, default-written behavior is preserved, D5 starts only in the high regime, dragging within high does not restart evolution, and sliding back down clears the audition override. Carry forward the pre-existing `getAudition()` state-shape quirk: `getAudition().candidate` reads falsy even when auditioning, though `enabled/candidateId/pattern` are correct.
- B2 development operators and branch-scope helpers are still duplicated between client/server layers. Acceptable while dev-only, but extract shared/recomputed modules before untrusted clients or trusted operator/lineage provenance.
- Future material-injecting actions still need the commit/lookahead path; the current pitch override should not become a backdoor for adding notes or changing motif shape.
- Future band-level changes such as key, mode, chord sequence, or song section changes should use a coordinated band proposal/song-sketch path, not private per-player intent.
- Model picker is convenience, not a pre-Byte-11 blocker; env/input/`window.ollama.setConfig()` already cover model selection.
- The current Ollama proxy is a Vite dev middleware. When SQLite/persistence or a durable backend lands, re-host the same proxy protocol in a standalone local server rather than changing the thought contract.
- Ollama API note: for `gemma4:31b`, include `think: false` in short structured `/api/chat` calls or the response may put reasoning in `message.thinking` and leave `message.content` empty.
- Byte L0a review focus: confirm the new Grow language layer is additive only. `MODE_INTERVALS` was exported but not changed; the bridge intentionally covers only ionian/mixolydian/dorian/aeolian even though current `main` already has lydian/phrygian in the engine. L0b should apply names/colors in UI; the future missing-mode byte should decide Helium/Scorch/Freefall engine support.
- Byte L0a-b review focus: confirm the vocabulary skin now covers every current engine-realized mode (`lydian`, `ionian`, `mixolydian`, `dorian`, `aeolian`, `phrygian`) while still leaving `Freefall`/locrian out. This should remain pure data/tests only; no UI application, tonal behavior, or SongGoal mode change.
- Byte L1a review focus: confirm `src/anchor-phrase.ts` is pure additive and unconsumed. The representation should use one absolute-beat coordinate system, 1-based Grow-language degrees, connector count exactly `anchors.length - 1` within a segment, no connector between segments, and real gaps between segments as breaths. Confirm numeric fields clamp with reported clamps, unknown kernels and structural violations reject, and the chosen caps are explicit and reasonable for L1b rendering.
- Byte L1b review focus: confirm `renderAnchorPhrase()` is pure and additive, emits only integer engine `scaleDegree` values (never raw pitch), and applies the 1-based-to-0-based conversion on anchors at render time. Check that connector notes start strictly after the previous anchor end and before the next anchor start, `fill`/`approach`/`detour` respond deterministically to density/reach/bias/pull/skew, `color` is stored but ignored, `orbit`/`skip` are safe fallbacks, and the read-only `window.anchorPhrase` demo does not create an audition/playback path.
- Byte L1c review focus: confirm `orbit` and `skip` are now real kernel renderers, not fallbacks, while `fill`/`approach`/`detour` stay behaviorally unchanged. `orbit` should decorate around the from-anchor degree with alternating neighbor motion and bias-controlled leading direction; `skip` should emit non-adjacent arpeggiated degree motion. Both should remain deterministic, integer-degree-only, in-window, ghosted, capped at 16 connector notes, diatonic/color-ignored, and purely additive with no playback or scoring consumer.
- Byte L0b review focus: confirm only the two musical mode readouts use evocative names (`control-key-readout`, `listening-tonal-context`), each retains `data-mode-classical` plus the hover title bridge, and classical SongGoal logs/summaries stay untouched. Confirm the degree-color legend is mounted inside the Inspect drawer while collapsed, uses the L0a-b degree roles/colors, and remains static/display-only with no note-degree coloring or playback/scoring behavior change.
- Byte L1d review focus: confirm `generateProsodicMelody()` still returns the existing flat `PlayerPatternSource` shape while internally using `generateProsodicAnchorPhrase()` plus `renderAnchorPhrase()`. The native phrase should have two segments, a real inter-segment breath, 1-based language degrees, pickup anchors, antecedent cadence degree 5, consequent cadence degree 1, and validated connector counts. Confirm candidate-store/scoring/evolution/playback/transport consumers are untouched, rendered notes stay in-scale, and the audible melody still reads as prosody even though it is no longer the old flat-note construction.
- Byte L1e review focus: confirm phrase candidates now store native `anchor-phrase/v1` genomes from `produceProsodyCandidates()` while legacy flat `PlayerPatternSource` genomes remain valid. Check that every consumer that needs the old flat shape (`scoreProsody`, candidate diversity, B2 development choices, D2 audition/evolving performance) goes through `renderPhraseCandidateGenome()` rather than casting the stored genome. Confirm the server independently normalizes/bounds anchor phrase genomes and that no SQLite schema, transport, scoring algorithm, playback scheduler, or candidate-selection logic changed. Carry forward: the anchor phrase genome validator is duplicated between client TypeScript and `server/persistence.mjs`; acceptable for dev-only persistence, but extract/share before untrusted clients or trusted candidate provenance.
- Byte L1e request-changes follow-up focus: verify the native no-op fix specifically. `createProsodyDevelopmentMutation()` should compare converted native replacements to the native parent before posting `phrase.replace`, and `postPersistenceJson()` should preserve server error bodies so no-op development errors remain catchable. Full smoke has passed 70/70 twice after the fix; Claude should re-check the formerly failing matched-diversity same-seed-in-one-store test path.
- Byte L2 review focus: confirm the graphical phrase editor is read-only and UI-only. Melody card click/Enter should open the overlay; close button, Escape, and backdrop should dismiss it; opening/closing must not call transport/prosody/persistence paths. The overlay should render the same current-song native prosody phrase exposed by `window.anchorPhrase.fromProsody()`, repaint on `song.setId`, use evocative mode naming plus canonical `data-mode-classical`, color anchors through `--degree-1..7`, show connector kernel labels and the inter-segment breath, and remain outside the Inspect drawer. Carry forward: L2 still shows the prosody idea even when candidate audition/evolving performance is active.
- Byte L3a review focus: confirm anchor editing is opt-in and in-session only. `window.phraseEditor.editAnchor()` should be the single mutation path for DOM gestures and debug calls; it must snap/clamp via `normalizeAnchorPhrase`, preserve segment order/breath gaps, and keep degrees 1..7. The audible path should be only `editorMelodyOverride -> getActiveMelodyPhrasing() -> existing refreshLookaheadSchedule()`, with higher precedence than candidate audition/prosody but no new scheduler/audio path. Revert, close, song change, and entering the evolving regime must clear the override and restore generated/candidate behavior; editing should be disabled while the written/evolving dial is in `evolving`.
- Byte L3b is approved and merged locally. Carry forward for later editor bytes: connector `color` is still stored-but-not-rendered until a chromatic-render byte, phrase editor DOM data hooks may need finer `data-*` attributes if L3c/L4 need more precise targeting, and all editor changes remain in-session only until L4 persistence/authoring.
- Byte L3c is approved and merged locally. The L3 editing arc is complete. Wait for Claude's L4 kickoff before building persistence/authoring/catalog work; L4 will re-touch persistence and the evolution loop, so the guardrails need to be pinned first. Carry forward: editor changes are still in-session only, and future editor bytes may need finer DOM `data-*` hooks.
- Byte L4a is approved and merged locally. L4b catalog review focus: authored branches (`editor-<songId>`) now persist both human-saved generation-0 ideas and any evolved population rows produced by running evolution over that branch. The catalog should deliberately distinguish "ideas I saved" from broader evolved descendants/purged rows, or filter by generation/lineage/status.
- Byte L4b review focus: confirm the catalog is read-only by default and scoped to `editor-<songId>`, with generated prosody plus non-purged saved/evolved candidates sorted by fitness. Browsing prev/next should only change the rendered roll and must not write candidates or change the audible override; Preview should be the only browse action that sets `editorMelodyOverride`, and Edit this idea should seed the L3 working phrase only for native `anchor-phrase/v1` genomes while legacy flat candidates remain view-only. Confirm no persistence endpoint/schema/server/scorer/transport/scheduler path changed.
- Byte L4b follow-up is resolved on the branch: the L4a/L4b smokes are now hermetic against reused `editor-lantern` branch contents and pass in a dirty local DB. Next byte should be the editor layout/discoverability cleanup Claude flagged from Arne's report: make the melody editor reachable without hunting in the drawer, constrain the overlay to the viewport with internal scroll, and reorganize the dense tool rows before L4c.
- Byte editor-ux-cleanup review focus: confirm this is presentation/entry/disclosure only. The visible Melody entry should open a compact player menu, and Graphical phrase should open the same editor without touching transport. The overlay should stay inside the viewport with internal scrolling, narrow width, no horizontal clipping, and no bottom spill. Default editor view should remain minimal; Anchor/Connector panels should be collapsed until selection, Connector More should hide bias/pull/skew, and the catalog list should stay inside the editor. Re-check that `window.phraseEditor` semantics, edit-model functions, catalog persistence, render adapter, scorer, transport, scheduling, and server code are unchanged. Full smoke passed twice on a reused DB to pin the L4 smoke hermeticity fix.
- Byte L4c-2 review focus: confirm the Evolution panel is read-only/presentation-only inside the existing melody editor disclosure model. It should read the same `editor-<songId>` phrase candidate rows as the catalog, summarize unfiltered rows so purged counts remain visible, and leave the browse catalog filtered to non-purged rows. Verify best/mean fitness sparkline points and status tally match `listCandidates`, sparse/no-evolution state is graceful, `window.phraseEditor.getEvolution()` is inspect-only, and no evolution algorithm, persistence endpoint/schema, scorer, renderer, transport, scheduler, or candidate-store logic changed. Validation note: a second full smoke run had transient unrelated failures in older editor/transport tests, then the failed slice passed 4/4 and a confirmation full smoke passed 78/78.
- Byte L4c-2 is approved and merged; the L-arc is complete. Future extension: the Evolution panel currently scopes only to `editor-<songId>` authored/evolved ideas, not the autonomous `dial-<songId>` population from the Line dial. Keep watching for rare full-smoke editor/transport timing flakes; none recurred in Claude's review, and the byte touches no transport path.
- Song Library slice review focus: confirm the canned starter materials are no longer exposed as the primary top-bar song surface, while the internal `SongId` templates remain safely hidden underneath for compatibility. Verify creating/renaming/navigating songs uses the existing song-change cleanup path, active title surfaces in status/sketch/editor displays, and per-library-song branches (`editor-<librarySongId>`, `dial-<librarySongId>`) isolate saved ideas/evolution. Carry forward: this is only the library front door; starter materials still provide the underlying loops, so the next musical arc should let songs own richer progression/key/tempo/form data instead of only choosing a hidden starter template.
- Song Starter review focus: confirm `New song` asks for a real starter brief before creating a song, derives setup only through bounded `SongGoal` fields, stores prompt/player plans on the song library entry, and applies tonic/mode/tempo/form via the existing `applySongGoalSetup()` refresh path. Player checkboxes/briefs are intentionally metadata-only in this slice; a future roster/scheduling byte should decide how selected players actually include/exclude transport lanes and how new player roles join the registry. The next musical gap remains richer song-owned progression over time: chord plan/key/tempo/form data should stop depending on the hidden canned templates.
- Song Starter follow-up review focus: confirm the new Generate step is a real visible seed, not just a pretty preview. `Generate starter` should be required before create, display setup/material/structure/player seed details, update player brief inputs, persist `baseSongId`/`materialSeed`/`structureSummary`, and make `window.anchorPhrase.fromProsody()` differ across different prompt-generated songs through the stored material seed. Still carry forward: generation is deterministic keyword-based for now, not Ollama-authored, and selected player plans still do not change the transport roster.
- Song Starter material review focus: confirm starter-generated songs now drive the actual transport/sketch/form/melody-scoring material through `getCurrentSongMaterial()`/`TransportHandlers.songMaterial`, not just titles or prosody seeds. The active material should be a deterministic 16-beat pulse/bass/melody phrase pack derived from starter goal/materialSeed/player plans, with the default canned material preserved as fallback. Listen specifically for whether this breaks the two-bar loop fatigue enough to justify continuing toward song-owned progression; carry forward that this is still deterministic keyword material, not Ollama-composed structure, and player checkboxes still silence/generated lanes only rather than changing the runtime roster.
